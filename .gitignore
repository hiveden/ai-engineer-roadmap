# ─────────────────────────────────────────────
# ai-engineer-roadmap · .gitignore
# 哲学：git 是"已验证学习进度"的投影，不是写作过程的备份
# ─────────────────────────────────────────────

# === assets 整体不入 git（原始素材、草稿、legacy-projects 等都是本地工作区）===
assets/

# === 私人 / 本地 / 临时 ===
.gemini_chats_backup/
.claude/
.aider*

# === IDE / 编辑器 ===
.idea/
.vscode/
*.swp
*.swo
*~

# === 系统文件 ===
.DS_Store
Thumbs.db

# === git 备份（reinit 时用的临时目录） ===
.git.bak/

# === Python 运行时（未来 code-demos 会用到） ===
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
env/
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# === Node 运行时（未来 demo 可能涉及） ===
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# === Marimo 自动生成（session 状态）===
**/__marimo__/

# 注：grid layouts（layouts/*.grid.json）入库——是录屏 / 视频 pipeline 的关键产物（见 _0-workflow.md §5）
# **/demos/layouts/   ← 已注释，恢复入库

# === 模型文件（大文件，应走 git-lfs 或外部存储） ===
*.onnx
*.pmml
*.pkl
*.pt
*.pth
*.h5
*.bin

# === PPT 源文件（章资源不入 git；已 tracked 的 02-LR/03线性回归.pptx 不受此影响）===
*.pptx

# === 录屏 pipeline 产物（大量 PNG / 视频 binaries + WIP 探针脚本）===
**/recording/
**/_recording/

# === scratch / sandbox（本地实验，非教材产物）===
sandbox/
