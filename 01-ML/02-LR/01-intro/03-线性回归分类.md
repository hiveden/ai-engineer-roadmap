---
tags: [回归/线性回归, 概念/一元, 概念/多元, 概念/多项式]
---

# 线性回归分类

> 维度：概念
> 知识点级别：【知道】一元 = 1 个 w；多元 = n 个 w；多项式 LR 也算线性回归（对 w 线性）
> 章节底稿全文见 [`README.md`](./README.md)（PPT slide 8, 10-11）

## ━━━━━━━━ 底稿 ━━━━━━━━

### PPT

> Slide 8 · 线性回归分类

- **一元线性回归**：y = wx + b（目标值只与一个因变量有关系）
- **多元线性回归**：

| | 面积 | 位置 | 楼层 | 朝向 | 价格 |
|---|---|---|---|---|---|
| 数据 1 | 80 | 1 | 3 | 0 | 81 |
| 数据 2 | 100 | 2 | 5 | 1 | 121 |
| 数据 3 | 80 | 3 | 3 | 0 | 102 |
| … | | | | | |
| 数据 n | 90 | 2 | 4 | 1 | ？ |

> Slide 10 · 思考题

> 这些模型是否是线性回归模型？
>
> 图：含 $y = ax^2 + bx + c$ 等多个候选（PPT 原图）

> Slide 11 · 补充：多项式线性回归

> 多项式回归（如 $y = w_1 x + w_2 x^2 + b$）在**权重 w 的角度仍是线性的**（w 的一次幂），属于线性回归范畴，可用正规方程或梯度下降求解。

### 笔记

> 【知道】线性回归分类

- **一元线性回归**：y = kx + b，目标值只与一个因变量有关系
- **多元线性回归**：目标值与多个因变量有关系

---

## ━━━━━━━━ 讲解 ━━━━━━━━

### 直觉

一句话：**一元 = 一根旋钮（k）+ 偏置（b）；多元 = 多根旋钮（w₁, w₂, ...）+ 偏置**。

教学意义上分一元/多元，工程上没区别——多元就是把 $kx$ 换成 $\mathbf{w}^T \mathbf{x}$，公式更简洁。

### 一元 vs 多元的本质区别

| 维度 | 一元 | 多元 |
|---|---|---|
| **公式** | $y = kx + b$ | $y = \mathbf{w}^T \mathbf{x} + b$ |
| **几何** | 2D 平面上一条**直线** | n+1 维空间一个**超平面** |
| **参数** | 2 个（k, b） | n+1 个（n 个 w + 1 个 b） |
| **可视化** | 散点 + 拟合直线（人眼可读） | 高维不可视；2D 投影只能看片面 |
| **正规方程** | 闭式公式 $k = \frac{\sum (x_i - \bar{x})(y_i - \bar{y})}{\sum (x_i - \bar{x})^2}$ | 矩阵公式 $\mathbf{w} = (X^T X)^{-1} X^T \mathbf{y}$ |
| **典型例** | 身高 → 体重 | 房价 = 面积+位置+楼层+朝向+... |

### 多项式线性回归（PPT slide 11 补充）

容易困惑：$y = w_1 x + w_2 x^2 + w_3 x^3 + b$ 是不是非线性？

**答案：仍然是线性回归**。原因：

- "线性"指的是**对权重 w 线性**（每个 $w_i$ 一次幂）
- 对**特征 x** 可以是任何形式：$x^2, \sqrt{x}, \log x, \sin x$ 都行
- 把 $x^2, x^3$ 视为**新特征** $z_2, z_3$，原模型变成 $y = w_1 z_1 + w_2 z_2 + w_3 z_3 + b$ —— 标准线性回归

```python
# 多项式线性回归在 sklearn 中的标准做法
from sklearn.preprocessing import PolynomialFeatures
from sklearn.linear_model import LinearRegression
from sklearn.pipeline import Pipeline

model = Pipeline([
    ("poly", PolynomialFeatures(degree=3)),  # x → [1, x, x², x³]
    ("lr", LinearRegression()),               # 标准 LR 训练
])
model.fit(X, y)
```

→ 所以"我能拟合非线性数据吗？" 答案是 **能**——通过特征工程把非线性变换喂进 LR。这是 [`07a-overfit`](../07a-overfit/) 章节的核心：**多项式次数太高 → 过拟合**。

### 不是所有 "y = f(x)" 都是线性回归

反例：

| 模型 | 是不是线性回归 | 理由 |
|---|---|---|
| $y = ax + b$ | ✓ | 一元 LR |
| $y = ax^2 + bx + c$ | ✓ | 多项式 LR（对 a, b, c 线性） |
| $y = a \sin x + b \cos x + c$ | ✓ | 三角特征 LR（对 a, b, c 线性） |
| $y = e^{ax + b}$ | ✗ | 对 a, b **指数** 不线性（需要换成 logistic 回归 / 对数变换）|
| $y = \frac{1}{1 + e^{-(ax+b)}}$ | ✗ | sigmoid，是 [`03-LogReg`](../../03-LogReg/) 的范畴 |

→ "线性回归"的核心判据：**模型表达式对所有未知参数（w 和 b）只出现一次幂、加减组合**。

### 互动 demo

一元 vs 二元的几何对比直接复用 [`02-线性回归.md`](./02-线性回归.md#互动-demo) 那个 demo：

| demo | 对应本章哪段 |
|---|---|
| [`demos/k-to-w-migration.py`](./demos/k-to-w-migration.py)（端口 2735）| **一元 vs 多元**：左图 1D 直线 / 右图 2D 决策面 / 下图 3D Surface 平面，"锁 w₂=0" toggle 直接演示**二元退化为一元** |

> ⚠️ 多项式 LR 部分（如 $y = w_1 x + w_2 x^2 + b$）暂时没有专门 demo——但可以**借用** k→w demo 的二元图：
>
> 把 x₂ 滑块替换为 $x_1^2$（手动）、把 w₂ 解读为"x 平方项的权重"——demo 结构同构。
> （后续如果需要专门的"多项式次数滑块" demo，见 [`07a-overfit/demos/poly-degree-overfit.py`](../07a-overfit/demos/poly-degree-overfit.py)，那里 degree 1-12 可调，是多项式 LR 的完整可视化。）

> Sources：
> - PPT Slide 8, 10, 11
> - 笔记（README §01-intro 线性回归分类）
> - sklearn 文档 `PolynomialFeatures`
