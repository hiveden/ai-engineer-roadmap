# 矩阵

> 级别：【知道】 · 维度：数学 · 底稿见 [`README.md`](./README.md)（对应 slide 43-47）

## 底稿（精简）

- 矩阵 matrix：m 行 n 列的二维数组，记作 $A \in \mathbb{R}^{m \times n}$
- 加法 / 减法：对应位置相加减（要求同形状）
- **乘法**：$C_{ij} = \sum_k A_{ik} B_{kj}$，要求左矩阵列数 = 右矩阵行数（**最重要**）
- 转置 transpose：$(A^T)_{ij} = A_{ji}$，行列互换
- 方阵 + 单位阵 I（对角 1 其余 0）+ 逆矩阵 $A^{-1}$（满足 $A A^{-1} = I$，**不一定存在**）
- 性质：乘法**不**满足交换律；满足结合律；$(AB)^T = B^T A^T$

---

## 直觉（一句话）

**矩阵 = 一堆向量打包**——既能装数据（每行一个样本），也能当「线性变换」用（一次变换一批向量）。

LR 训练的核心**一行公式**靠矩阵打底：$\mathbf{w} = (X^T X)^{-1} X^T \mathbf{y}$（正规方程）——所有样本、所有特征、所有训练计算压缩成一个表达式。

## 公式 + 例子

设 $A = \begin{bmatrix} 1 & 2 \\ 3 & 4 \end{bmatrix}$，$B = \begin{bmatrix} 5 & 6 \\ 7 & 8 \end{bmatrix}$。

**乘法**（$A \times B$，左行 × 右列）：

$$AB = \begin{bmatrix} 1 \cdot 5 + 2 \cdot 7 & 1 \cdot 6 + 2 \cdot 8 \\ 3 \cdot 5 + 4 \cdot 7 & 3 \cdot 6 + 4 \cdot 8 \end{bmatrix} = \begin{bmatrix} 19 & 22 \\ 43 & 50 \end{bmatrix}$$

**形状要对得上**：$A \in \mathbb{R}^{m \times k}$，$B \in \mathbb{R}^{k \times n}$ → $AB \in \mathbb{R}^{m \times n}$。中间的 k 必须相等，**不相等就乘不了**。

**转置**：

$$A^T = \begin{bmatrix} 1 & 3 \\ 2 & 4 \end{bmatrix}$$

**单位阵**（对角 1，其余 0，方阵的「乘法 1」）：

$$I_2 = \begin{bmatrix} 1 & 0 \\ 0 & 1 \end{bmatrix}, \quad A I = I A = A$$

**逆矩阵**（仅方阵且行列式非零才存在）：

$$A^{-1} = \frac{1}{\det A} \begin{bmatrix} 4 & -2 \\ -3 & 1 \end{bmatrix} = \begin{bmatrix} -2 & 1 \\ 1.5 & -0.5 \end{bmatrix}$$

验证：$A A^{-1} = I$。

## 与 LR 的连接

**矩阵化 LR**：m 个样本，n 个特征。

| 对象 | 形状 |
|---|---|
| 特征矩阵 X（每行一个样本，每列一个特征 + 末列全 1 吸收偏置 b）| $m \times (n+1)$ |
| 权重向量 $\mathbf{w}$（含偏置）| $(n+1) \times 1$ |
| 标签向量 $\mathbf{y}$ | $m \times 1$ |
| 预测向量 $\hat{\mathbf{y}} = X\mathbf{w}$ | $m \times 1$ |

**正规方程**（normal equation，[`04a-analytical/`](../04a-analytical/) 主菜）：

$$\boxed{\mathbf{w} = (X^T X)^{-1} X^T \mathbf{y}}$$

**这一行公式 = LR 整套训练**：把 m 个样本、n 个特征、所有偏导和方程组解压缩成 4 个矩阵操作（转置 → 乘 → 求逆 → 再乘）。numpy 里就是一行：

```python
w = np.linalg.inv(X.T @ X) @ X.T @ y
```

**梯度下降**（[`04b-gd/`](../04b-gd/)）也用矩阵——梯度 $\nabla L = \frac{2}{m} X^T (X\mathbf{w} - \mathbf{y})$ 是矩阵表达式，每次迭代一行代码。

## 常见混淆 / 坑

- **乘法不满足交换律**：$AB \neq BA$（一般情况）。形状对得上 $AB$ 能算，反过来 $BA$ 可能形状都不对。**写顺序不能乱换**。
- **矩阵不一定可逆**——$X^T X$ 不可逆的情况：(1) 特征数 > 样本数；(2) 特征间存在线性相关（共线性）。这时正规方程失效，得用伪逆 `np.linalg.pinv` 或加 L2 正则（Ridge 回归就是塞 $\lambda I$ 强行可逆）。
- **求逆是 $O(n^3)$ 的昂贵操作**——n 大（比如 10000 个特征）时正规方程跑不动，所以工业界默认用梯度下降（[`04b-gd/`](../04b-gd/)）。**正规方程只适合小 n**。
- **`@` vs `*` 在 numpy**——`A @ B` 是矩阵乘，`A * B` 是逐元素乘（要求形状相同或可 broadcast）。和向量章节的坑一样，**符号不同结果天差地别**。
- **转置乘法的反序律**：$(AB)^T = B^T A^T$（顺序反过来），新手常写成 $A^T B^T$ 错。证明正规方程时这条用得上。
