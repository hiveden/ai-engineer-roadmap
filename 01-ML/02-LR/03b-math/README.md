# 第 3 章 b · 损失函数 + 数学复习

> 本文件 = 章 PPT 完整底稿（复习记忆页）
> 维度：**概念 + 数学**
>
> **按知识点拆分的讲解版**：
>
> 1. [`01-损失函数.md`](./01-损失函数.md) — 【掌握】MSE 损失
> 2. [`02-数据表述.md`](./02-数据表述.md) — 【知道】常见数据表述
> 3. [`03-导数.md`](./03-导数.md) — 【知道】
> 4. [`04-偏导.md`](./04-偏导.md) — 【知道】
> 5. [`05-向量.md`](./05-向量.md) — 【知道】
> 6. [`06-矩阵.md`](./06-矩阵.md) — 【知道】

## 底稿

### PPT 原文（slide 18-52）

#### Slide 18 · 线性回归的求解方法

> 通过线性回归 API 可快速找到拟合结果，那是怎么求解的呢？多个线性模型，哪个才是最优解？**如何评估模型的好坏？定量。**

> **备注**：眼睛能看，但如何定量？

#### Slide 19 · 引入评判标准 – loss function 损失函数

- **误差**：每个数据点预测值 y − 真实值 y
- **损失函数**：综合衡量"模型预测结果"和"真实值"之间的整体差异
- 也叫代价函数、成本函数、目标函数

> **备注**：在 172、174 处的身高，预测值与真实值之差。

#### Slide 20 · 损失函数数学表达

- 第 i 个误差的绝对值（→ MAE）
- **对误差的平方惩罚！！**（→ MSE）
- **不建议用在有异常值的数据**

> **备注**：对误差的平方惩罚——怕模型优化时为了降低 loss 去拟合异常值。

#### Slide 21 · 损失函数（结论）

- 结论：损失函数越小的模型，"模型预测结果"越接近"真实值"，模型性能越好
- 红色直线在每个数据点上有更小的误差、更小的 loss 值，因此它是更好的线性模型
- **最小二乘**：平方和误差

#### Slide 22 · 求解最优模型的套路

- 最优模型 ⇔ 最小的损失函数
- 通过损失函数最小化，求解 w、b → 获得最优模型参数
- 最优模型：y = wx + b

#### Slide 23 · 损失函数计算（简化）

- 损失函数是关于 k、b 的函数，展开会变成二元二次方程
- 为简化计算，先固定截距 b，b 固定成 -100

#### Slide 24 · 求损失函数最小值

PPT 原算（备注里）：

```
160² + 166² + 172² + 174² + 180² = 145416   → k² 系数
2*160*156 + 2*166*160.6 + ... + 2*180*175 = 281671.6
156.3² + 160.6² + 165.1² + 168.5² + 175² = 136496.32
L(k, b=-100) = 145416k² − 281671.6 k + 136496.32
```

- 当损失函数取最小值时，得到 k 就是最优解：k = 281671.6 / (2 × 145416) = **0.9685**
- 截距 b = -100 情况下，预测小明体重：y = 0.9685 × 176 − 100 = **70.456**

> 总结：求最好拟合线 y = kx + b ⇒ 引入损失函数 Loss(k, b) ⇒ 优化方法求最小值得 k 最优解

#### Slide 25 · Loss 曲线与模型参数

> 当模型参数 k 偏离最优点，则 loss 上升，拟合情况会变差。

#### Slide 26 · 线性回归问题求解需要什么？

| 要素 | 内容 |
|---|---|
| **样本数据** | 特征 x 和目标 y 组成，假设线性分布 |
| **线性回归模型** | 一元 y=kx+b / 多元 y=Σ wᵢxᵢ+b |
| **损失函数** | MSE 平均平方误差、MAE 平均绝对误差 等 |
| **优化方法** | 1) 梯度下降法（迭代逼近） 2) 正规方程法（求导/求偏导直接解） |

#### Slide 27 · 本节小结

- 1 线性回归 API：`estimator = LinearRegression()` / `fit(x, y)` / `predict([[4.0]])`
- 2 损失函数概念：误差 = 预测值 − 真实值
- 3 回归问题中的损失函数：衡量预测和真实差异，也叫代价函数 / 目标函数

#### Slide 28 · 自检题（损失函数 多选）

> 有关损失函数下列说法正确的是？
>
> A）损失函数（Loss Function）又被称为代价函数（Cost Function）
> B）损失函数可用来描述预测值的分布，看是否为均匀分布还是正态分布、或其他分布
> C）损失函数可用来描述输出（预测值）和观测结果（真实值）效果，可衡量模型效果好坏
> D）不同的任务比如分类、回归、聚类问题，一般会采用各自的损失函数
> E）线性回归求解一般需要数据、假设函数、损失函数、损失函数优化方法等部分，相互配合共同完成
>
> **答案：ACDE**

#### Slide 30 · 数学复习 学习目标

- 知道机器学习中常见的数据表述
- 知道什么是导数
- 知道什么是偏导
- 知道向量和矩阵的简单运算

#### Slide 31 · 基础数学 – 数据表述

- **标量** scalar：一个独立存在的数，只有大小没有方向
- **向量** vector：一列顺序排列的元素。默认是列向量。**有大小和方向**（模和夹角）
  - 例：张三的数理化成绩 [70, 80, 90]
- **矩阵** matrix：二维数组（m 行 n 列）
  - 例：张三、李四的数理化成绩 [[70, 80, 90], [75, 85, 95]]
  - m = 行数（样本数），n = 列数（特征数）
- **张量** Tensor：基于向量和矩阵的推广（更高阶数组）

> 宗旨：用到什么就学什么，不要盲目展开。

#### Slide 32-37 · 导数

> PPT 原图：导数定义、几何意义（切线斜率）、常见函数导数表（幂/指数/对数/三角）、导数四则运算、复合函数链式求导。

- **复合函数求导**：g(h) 是外函数，h(x) 是内函数。**先对外函数求导，再对内函数求导**

#### Slide 38 · 导数求极值

- **导数为 0 的位置是函数的极值点**

> **备注**：-b/2a 怎么来的？（一元二次方程顶点 x 坐标）

#### Slide 39-40 · 偏导

> 多元函数对单变量求导，把其他变量当常数。详见笔记部分公式。

#### Slide 41 · 向量运算

- 几何意义：向量 (1,1) / (1,2)
- 向量基本运算（加减、点积）
- 向量是有大小和方向（模和夹角）
- 向量矩阵转置 Transpose：T_ij ↔ T_ji，m×n → n×m
- 机器学习领域：**默认是列向量**

#### Slide 42 · 范数 Norm

- 范数（norm）是数学中的一种基本概念，**具有长度的意义**
- **L1 范数**：向量中各个元素绝对值之和
- **L2 范数**：向量的模长，每个元素平方求和再开平方根
- **Lp 范数**：向量中每一个元素 p 次幂求和，再开 p 次根

#### Slide 43-47 · 矩阵运算

- 矩阵：m 行 n 列的数据
- 加法 / 减法：对应位置相加减
- **乘法**：对应行列元素相乘后求和（**非常重要**）
- **方阵**：行数 = 列数
- **单位阵** I（或 E）：主对角线为 1，其他为 0；A @ I = I @ A = A
- **矩阵的逆**：不一定存在！
- **乘法不满足交换律**：A×B ≠ B×A（仅极少特殊情况 AB=BA）
- **乘法满足结合律**：A × (B × C) = (A × B) × C
- 转置：(A^T)_ij = A_ji，性质 (AB)^T = B^T A^T

#### Slide 48-49 · 数学复习小结

1. 标量、向量、矩阵、张量概念
2. 导数：函数上某点切线就是导数（瞬时变化率）
3. 导数求解：常数 / 指数 / 幂 / 三角都有导函数（**会查表应用**）；复合函数先外后内
4. 利用导数求极值：导数为 0 即极值点
5. 偏导数 + 向量 / 矩阵基本运算

#### Slide 50 · 自检题（导数与极值）

> 1. 已知函数 f(x,y)，x、y 均为变量，那么 f(x,y) 的偏导数 df/dx 是？
>    A. f(x,y) 的导数 / B. f(x,y) 的一阶偏导数 / C. f(x,y) 的二阶偏导数 / D. f(x,y) 对 x 的偏导数
>
> **答案：D**
>
> 2. 已知函数 f(x)，令 g(x) = f'(x)，则 g'(x) = ？
>    A. f''(x) / B. f'(x) / C. f(x) / D. 不能确定
>
> **答案：A**
>
> 3. 已知函数 f(x)，若在 x=a 时函数取极小值，则？
>    A. f'(a) = 0 / B. f'(a) > 0 / C. f'(a) < 0 / D. 不能确定
>
> **答案：A**

#### Slide 52 · 自检题（矩阵）

> **答案：B**（A 矩阵条件错应中间一致；B 对；C 错；D 错——有些矩阵不一定有逆）

---

### 笔记（已整理）

> 03 · 损失函数 + 复习导数和矩阵

> 【掌握】损失函数

需要设置一个评判标准。

**误差概念**：用预测值 y – 真实值 y 就是误差。

**损失函数**：衡量每个样本预测值与真实值效果的函数。

"红色直线能更好的拟合所有点" 也就是误差最小，误差和最小。

损失函数数学如何表达呢？又如何求损失函数的最小值呢？

当损失函数取最小值时，得到 k 就是最优解。

想求一条直线更好的拟合所有点 y = kx + b：

- 引入损失函数（衡量预测值和真实值效果）Loss(k, b)
- 通过一个优化方法，求损失函数最小值，得到 K 最优解

回归的损失函数：

- **均方误差**（Mean-Square Error, MSE）
- **平均绝对误差**（Mean Absolute Error, MAE）

> 【知道】常见的数据表述

- 为什么要学习标量、向量、矩阵、张量？
  - 因机器学习、深度学习中经常用，不要因是数学就害怕
  - 宗旨：用到就学什么，不要盲目的展开、大篇幅学数学
- **标量** scalar：一个独立存在的数，只有大小没有方向
- **向量** vector：向量指一列顺序排列的元素。默认是列向量
- **矩阵** matrix：二维数组
- **张量** Tensor：多维数组，张量是基于向量和矩阵的推广

> 【知道】导数

当函数 y = f(x) 的自变量 x 在一点 $x_0$ 上产生一个增量 Δx 时，函数输出值的增量 Δy 与自变量增量 Δx 的比值在 Δx 趋于 0 时的极限 a 如果存在，a 即为在 $x_0$ 处的导数，记作 $f^\prime(x_0)$ 或 df($x_0$)/dx。

导数是函数的局部性质。一个函数在某一点的导数描述了这个函数在这一点附近的变化率。

函数在某一点的导数就是该函数所代表的曲线在这一点上的切线斜率。

**LR 用得到的两条**：
- 幂函数：$(x^\alpha)^\prime = \alpha x^{\alpha-1}$（求 MSE 关于 w 的导数靠它）
- 链式法则：$\{g[h(x)]\}^\prime = g^\prime(h) \cdot h^\prime(x)$（嵌套函数求导）

**导数求极值**：导数为 0 的位置是函数的极值点（LR 闭式解的核心思路）。

> 完整导数公式表 / 四则运算法则不展开——遇到具体函数查表即可。

> 【知道】偏导

**多变量函数对其中一个变量求导**，把其它变量当常数。

设 $f(x, y) = x^2 + 3xy + y^2$：

- 对 x 偏导：$\frac{\partial f}{\partial x} = 2x + 3y$（y 当常数 → $3xy \to 3y$，$y^2 \to 0$）
- 对 y 偏导：$\frac{\partial f}{\partial y} = 3x + 2y$

**线性回归用偏导做什么**：MSE 损失 $L(w, b) = \sum (y - wx - b)^2$ 是 w 和 b 的二元函数，
分别对 w、b 求偏导，令偏导 = 0，就得到正规方程的解析解（见 04a）。

**梯度**：所有偏导组成的向量，记作 $\nabla L = \left[\frac{\partial L}{\partial w_1}, \frac{\partial L}{\partial w_2}, \ldots\right]^T$。
梯度方向 = 函数上升最快的方向，**反方向**就是梯度下降的方向（见 04b）。

> 【知道】向量

**向量加法 / 减法**（同维度逐元素）：

$$\mathbf{a} + \mathbf{b} = [a_1 + b_1, a_2 + b_2, \ldots, a_n + b_n]^T$$

**点积 / 内积**（结果是标量）：

$$\mathbf{a} \cdot \mathbf{b} = \mathbf{a}^T \mathbf{b} = \sum_{i=1}^{n} a_i b_i$$

**向量 L2 范数**（向量长度）：

$$\lVert \mathbf{a} \rVert_2 = \sqrt{\sum a_i^2} = \sqrt{\mathbf{a}^T \mathbf{a}}$$

**线性回归用向量做什么**：把 n 个特征 $x_1, x_2, \ldots, x_n$ 写成列向量 $\mathbf{x}$，
权重 $\mathbf{w}$ 同维列向量，预测值就是 $\hat{y} = \mathbf{w}^T \mathbf{x} + b$，一行公式覆盖所有维度。

> 【知道】矩阵

**矩阵乘法**（A: m×k, B: k×n → C: m×n）：

$$C_{ij} = \sum_{p=1}^{k} A_{ip} B_{pj}$$

**转置**：$(A^T)_{ij} = A_{ji}$，行变列、列变行。

**逆矩阵**：仅方阵且满秩可逆，满足 $A A^{-1} = I$（单位阵）。

**线性回归用矩阵做什么**：把 m 个样本叠成矩阵 X (m×n)，标签叠成向量 y (m×1)，
则模型变成 $\hat{\mathbf{y}} = X \mathbf{w}$。MSE 损失展开后求导得正规方程：

$$\mathbf{w} = (X^T X)^{-1} X^T \mathbf{y}$$

这个公式是 04a 的核心——**所有训练计算用一个矩阵公式搞定**，是 LR 解析解的精华。
