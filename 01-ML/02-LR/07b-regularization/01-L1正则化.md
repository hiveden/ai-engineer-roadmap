# L1 正则化（Lasso）

> 级别：【理解】
> 维度：数学 + 代码
> 章节底稿全文见 [`README.md`](./README.md)（PPT slide 109-114 + 笔记）

## 底稿

在原损失 $L(W)$ 后加上**所有权重的绝对值之和**作为惩罚项：

$$L = L(W) + \lambda \sum_{i=1}^{n} \lvert w_i \rvert$$

- $\lambda$（lambda）= 惩罚系数，越大压得越狠
- 几何形状：$y = \lvert x \rvert$ 是 V 字形，多维下是**菱形**（顶点在坐标轴上）
- 对 $w_i$ 求导用 sign 函数：$\frac{\partial L}{\partial w_i} = \frac{\partial L(W)}{\partial w_i} + \lambda \cdot \text{sign}(w_i)$

**作用**：让部分 $w_i = 0$ → 产生**稀疏解** → 自动**特征选择**。

API：`from sklearn.linear_model import Lasso`

---

## 直觉

一句话：**L1 给每个权重一刀「等量」削——小权重一刀就归零，归零后就再也不动**。

## 几何直觉：为什么菱形会让权重归零

回忆带约束的优化：原损失等高线（椭圆）+ 约束区域（L1 是菱形）相切的点 = 最优解。

```
        w2
         |
         ◇          ← L1 约束区域（菱形，顶点在坐标轴上）
        /│\
       / │ \
  ----+--+--+----- w1
       \ │ /
        \│/
         ◇
         |
```

**关键观察**：菱形有 4 个**尖锐顶点**（在坐标轴上）。椭圆从外面收缩进来时，最容易和顶点相切——而顶点意味着**至少一个 $w_i = 0$**。

对比 L2 的圆形（光滑），切点几乎不会落在坐标轴上 → L2 不稀疏，L1 稀疏。

## 梯度更新规则：等量收缩

对比 [`04b-gd/`](../04b-gd/) 中纯 LR 的更新公式，L1 多出最右一项：

$$w_i \leftarrow w_i - \alpha \cdot \frac{\partial L(W)}{\partial w_i} - \alpha \lambda \cdot \text{sign}(w_i)$$

| 当前 $w_i$ | $\text{sign}(w_i)$ | 惩罚项做的事 |
|---|---|---|
| > 0 | +1 | 减一个**常数** $\alpha\lambda$ → 往 0 推 |
| < 0 | -1 | 加一个**常数** $\alpha\lambda$ → 往 0 推 |
| = 0 | 0 | 不动 → **一旦归零永久归零** |

→ 这是「**等量收缩**」（每步减/加同一个常数），与 L2 的「按比例收缩」本质不同。

**为什么等量收缩能产生稀疏？** 因为对一个本来就很小的 $w_i$（比如 0.01），减一个常数（比如 0.005）几次就归零了；归零后 sign 函数是 0，惩罚项不再推动它——**死透了**。

## sklearn API

```python
from sklearn.linear_model import Lasso

# alpha 就是公式里的 lambda
lasso = Lasso(alpha=0.1, max_iter=10000)
lasso.fit(X_train, y_train)

print(lasso.coef_)  # 大量 0 → 稀疏解
```

> 注意：sklearn 用 `alpha` 命名（不是 lambda），因为 lambda 是 Python 关键字。

完整 demo + 三方案对比见 [`03-正则化案例.md`](./03-正则化案例.md) 和 [`demos/l1-l2-shrinkage.py`](./demos/l1-l2-shrinkage.py)。

## 用于特征选择

工程价值：你扔进去 100 个特征，Lasso 训练完直接告诉你「这 80 个无关，扔掉」。

```python
selected = X.columns[lasso.coef_ != 0]
print(f"L1 保留了 {len(selected)} / {len(X.columns)} 个特征")
```

适用场景：

- 特征维度高（> 样本数）
- 怀疑很多特征是噪声 / 冗余
- 需要可解释模型（「就这几个特征重要」比「100 个特征都有点作用」好讲故事）

## 配套阅读

- 为什么需要正则：[`07a-overfit/`](../07a-overfit/) 过拟合诊断
- L1 vs L2 对比：[`02-L2正则化.md`](./02-L2正则化.md)
- 评估正则效果：[`05-metrics/`](../05-metrics/) MSE
