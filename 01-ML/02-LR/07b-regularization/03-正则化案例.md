# 正则化案例：LR / Lasso / Ridge 三方案对比

> 级别：【实操】
> 维度：代码
> 配套 demo：[`demos/l1-l2-shrinkage.py`](./demos/l1-l2-shrinkage.py)

## 任务设定

构造抛物线 + 噪声数据，用 **10 次多项式特征** 故意制造过拟合，再分别用 LR / Lasso / Ridge 拟合，对比：

1. 训练 / 测试 MSE（看泛化）
2. 权重系数（看哪些特征被「关掉」）

> 真实分布：$y = 0.5x^2 + x + 2 + \mathcal{N}(0, 1)$
> 所以理想模型应该只用 $x$ 和 $x^2$，$x^3 \cdots x^{10}$ 都该 = 0 或接近 0。

## 数据准备

```python
import numpy as np
from sklearn.linear_model import LinearRegression, Lasso, Ridge, LassoCV, RidgeCV
from sklearn.preprocessing import PolynomialFeatures, StandardScaler
from sklearn.pipeline import Pipeline
from sklearn.model_selection import train_test_split
from sklearn.metrics import mean_squared_error

np.random.seed(666)
x = np.random.uniform(-3, 3, size=100).reshape(-1, 1)
y = 0.5 * x.ravel()**2 + x.ravel() + 2 + np.random.normal(0, 1, 100)

X_train, X_test, y_train, y_test = train_test_split(x, y, random_state=5)
```

## 方案 1：纯 LR + 10 次多项式（过拟合基线）

```python
poly_lr = Pipeline([
    ("poly", PolynomialFeatures(degree=10)),
    ("scaler", StandardScaler()),
    ("lr", LinearRegression()),
])
poly_lr.fit(X_train, y_train)
print("LR  train MSE:", mean_squared_error(y_train, poly_lr.predict(X_train)))
print("LR  test  MSE:", mean_squared_error(y_test,  poly_lr.predict(X_test)))
print("LR  coef    :", poly_lr.named_steps["lr"].coef_)
```

**预期观察**：

- `train MSE` 很低（如 0.7-0.8）；`test MSE` 显著更高（如 1.5-3.0）→ 典型过拟合
- 权重数值波动大，部分高次项绝对值大（模型试图用 $x^9$、$x^{10}$ 拟合噪声）

## 方案 2：Lasso（L1）→ 高次项归零

```python
poly_lasso = Pipeline([
    ("poly", PolynomialFeatures(degree=10)),
    ("scaler", StandardScaler()),
    ("lasso", Lasso(alpha=0.1, max_iter=10000)),
])
poly_lasso.fit(X_train, y_train)
print("Lasso coef:", poly_lasso.named_steps["lasso"].coef_)
print("Lasso test MSE:", mean_squared_error(y_test, poly_lasso.predict(X_test)))
```

**预期观察**：

- 权重数组里**大量精确的 0**（如 $x^3$ 到 $x^{10}$ 全 0），只有 $x^1, x^2$ 非零 → 完美的「特征选择」
- `test MSE` 接近 1.0（接近真实噪声标准差 $\sigma^2 = 1$）

## 方案 3：Ridge（L2）→ 高次项变小但不为 0

```python
poly_ridge = Pipeline([
    ("poly", PolynomialFeatures(degree=10)),
    ("scaler", StandardScaler()),
    ("ridge", Ridge(alpha=1.0)),
])
poly_ridge.fit(X_train, y_train)
print("Ridge coef:", poly_ridge.named_steps["ridge"].coef_)
print("Ridge test MSE:", mean_squared_error(y_test, poly_ridge.predict(X_test)))
```

**预期观察**：

- 权重**全部非零**，但都被压成很小的值（高次项尤其小）
- `test MSE` 也接近 1.0，与 Lasso 相当

## 三方案对比表

| 方案 | train MSE | test MSE | 高次项权重 | 解释力 |
|---|---|---|---|---|
| LR | 极低 | 高（过拟合） | 大且乱 | 差 |
| Lasso | 略高 | 低 | **= 0** | 强（明确告诉你哪些特征有用） |
| Ridge | 略高 | 低 | 很小但 ≠ 0 | 中（每个特征都有点作用） |

→ 评估指标见 [`05-metrics/`](../05-metrics/)；过拟合症状见 [`07a-overfit/`](../07a-overfit/)。

## alpha 调参（CV 自动搜）

`alpha`（即公式里的 $\lambda$）是关键超参——太小没效果，太大欠拟合。手动试不如 CV：

```python
# Lasso 自动 CV
lasso_cv = LassoCV(alphas=np.logspace(-4, 1, 50), cv=5, max_iter=10000)
lasso_cv.fit(X_train_poly, y_train)
print("最优 alpha:", lasso_cv.alpha_)

# Ridge 自动 CV
ridge_cv = RidgeCV(alphas=np.logspace(-4, 2, 50), cv=5)
ridge_cv.fit(X_train_poly, y_train)
print("最优 alpha:", ridge_cv.alpha_)
```

`alpha` 与误差关系是 **U 形曲线**：

- alpha → 0：退化为纯 LR，过拟合，test MSE 高
- alpha 适中：test MSE 最低（甜点区）
- alpha → ∞：所有权重压到 0，欠拟合，train + test MSE 都高

## ElasticNet：L1 + L2 混合

```python
from sklearn.linear_model import ElasticNet

en = ElasticNet(alpha=0.1, l1_ratio=0.5)  # l1_ratio=1 → Lasso；=0 → Ridge
en.fit(X_train_poly, y_train)
```

损失函数：

$$L = L(W) + \lambda \cdot \rho \sum \lvert w_i \rvert + \lambda \cdot \frac{1 - \rho}{2} \sum w_i^2$$

- 想要稀疏 + 稳定（Lasso 单用对相关特征不稳定）→ ElasticNet
- 不知道选哪个时的「保守默认」

## 完整可运行 demo

见 [`demos/l1-l2-shrinkage.py`](./demos/l1-l2-shrinkage.py)，含可视化（拟合曲线 + 权重柱状图）。
