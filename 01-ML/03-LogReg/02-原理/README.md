# 第 2 章 · 原理

> 维度：**算法 + 数学**
>
> **按知识点拆分的讲解版**：
>
> 1. [`01-原理.md`](./01-原理.md) — 【理解】Logistic Regression
> 2. [`02-损失函数.md`](./02-损失函数.md) — 【知道】对数似然损失

## ━━━━━━━━ 底稿 ━━━━━━━━

### PPT

> 从 [`../04逻辑回归.pptx`](../04逻辑回归.pptx) 提取（slide 12-22）。图占位标 〔图〕；排版整理；文字保留 PPT 原话。

> Slide 12 · 目录（章间导航）

- 逻辑回归简介（应用场景，数学知识）
- 逻辑回归原理
- 逻辑回归API函数和案例
- 分类问题评估（混淆矩阵、精确率、召回率、F1-score、AUC指标、ROC曲线）
- 电信客户流失预测案例

> Slide 13 · 学习目标

- 理解逻辑回归算法的原理
- 知道逻辑回归的损失函数

> Slide 14 · 逻辑回归原理 – 概念

逻辑回归概念 Logistic Regression

基本思想    目的：二分类

输入特征，利用线性模型 f(x) = w^T x + b 根据特征的重要性计算出一个得分 logits
再使用 sigmoid 函数将 f(x) 的输出值映射为概率值
设置阈值（eg：0.5），输出概率值大于 0.5，则将未知样本输出为 1类
否则输出为 0 类

逻辑回归的假设函数：

线性回归的输出，作为逻辑回归的输入，得到分类概率

h(w) = sigmoid(w^T x + b)

一种二分类模型，把线性回归的输出，作为逻辑回归的输入。
利用 sigmoid 函数，转化为（0, 1）之间的概率值

sigmoid函数

〔公式图：假设函数 h(w) = sigmoid(w^T x + b)〕

> **Notes**：应用场景  缺陷检测   垃圾邮件分类

> Slide 15 · 逻辑回归原理 – 概念（三部曲 + 例子）

三部曲：

1. 预测值y (logits) = w^T x + b
2. 分类概率(归一) = sigmoid(logits)
3. 设定阈值，判断二分类结果

举个例子：逻辑回归预测过程
（比如这里的阈值 (分界线) 为0.6）

```
特征1  特征2   特征3 → 线性模型 → 预测值/得分 → sigmoid函数 → 概率（归一）→ 判断阈值=0.6
样本1
样本2
样本3
样本4
样本5
```

〔图：5样本预测过程表格（特征→得分→概率→阈值判断）〕

用什么损失函数？

> Slide 16 · 逻辑回归原理 – 损失函数

逻辑回归原理 - 损失函数

二分类交叉熵损失函数：

Loss(L) = - ∑ᵢ₌₁ᵐ ( yᵢlog(pᵢ) + (1−yᵢ)log(1−pᵢ) )

pᵢ = sigmoid(w^T x + b) 是逻辑回归的概率

损失函数的工作原理：每个样本预测值只有两个类别时，预测的（归一）概率应当与标签靠拢

用 0、1 表示A/B两个类别

举个例子 – 损失函数手工计算

| 样本 | 预测→ | 真实 | 难易 |
|---|---|---|---|
| 样本1 | → A | | 难例 |
| 样本2 | → B | | 难例 |
| 样本3 | → A | | 难例 |
| 样本4 | → B | | 难例 |
| 样本5 | → A | | 易例 |

累计每个样本的二分类交叉熵loss

计算损失：=- [ 1log0.4 + (1-1)log(1-0.4) +            # 第1个样本产生的 二分类  损失值
                      0log0.6 + (1-0)log(1-0.68) +            # 第2个样本产生的 二分类  损失值
                      1log0.41 + (1-1)log(1-0.41) + ….

> **Notes**（讲师）：线性回归是累计每个样本的预测误差；逻辑回归是累计每个样本的预测概率的误差。算式中 0.6 与 0.68 不一致疑似 typo，保留原文。

> Slide 17 · 通过极大似然法推导二分类交叉熵损失函数

通过极大似然法     推导二分类交叉熵损失函数

1、1个样本的概率表示

假设：有 0、1 两个类别，某个样本被预测为 1 类的概率为 p, 则预测为 0 类的概率必然为 1-p，
则一个样本分类正确的概率为：

真实值

〔公式图：单样本概率 P = p^y (1-p)^(1-y)〕

等价

〔公式图：等价写法〕

2、n个样本的概率表示

假设：有样本 [(x₁, y₁), (x₁, y₂) …, (xₙ, yₙ)]，n个样本，所有样本都预测正确的联合概率为：

〔公式图：n样本联合概率 P = ∏ᵢ pᵢ^yᵢ(1-pᵢ)^(1-yᵢ)〕

pᵢ 表示每个样本单独被分类正确时的概率，比如 将样本x1准确预测为y1类别的概率是p1，以此类推
yᵢ 表示每个样本的真实类别（0或1）

极大似然法：即：模型的参数如何调整，才能使得联合概率P 最大？即对"每个样本的类别都预测准确"的可能性最高？

> **Notes**：比如预测明天是否下雨 40%，不下雨就是60%；当y就属于1类时，则正确率就是p；当y就属于0类时，则正确率就是1-p；既成事实

> Slide 18 · 似然函数求极大 – 方法1

极大似然法：即：模型的参数如何调整，才能使得联合概率P 最大？即对"每个样本的类别都预测准确"的可能性最高？

方法1   似然函数 ∏ᵢ pᵢ^yᵢ(1-pᵢ)^(1-yᵢ) 的导数为0，求极大值对应的参数

难以计算

〔公式图：直接求导难以计算〕

> Slide 19 · 梯度下降法 – 似然函数转对数似然

2  梯度下降法

为了方便计算，将似然函数 转 对数似然函数，取log优化函数：连乘形式转换为对数加法形式：

log(L) = ∑ᵢ₌₁ⁿ（ yᵢlog(pᵢ) + (1−yᵢ)log(1−pᵢ) )

pᵢ = sigmoid(w^T x + b) 是逻辑回归预测的二分类概率

log是单调函数

等号2边取log

2  梯度下降法只能求函数最小值问题，如何求解 似然函数的极大值？？

倒反天罡

〔公式图：取 log 后的对数似然形式〕

> **Notes**（讲师）：强调 log是单调函数。"倒反天罡"为讲师文案，指 max → 加负号变 min。

> Slide 20 · 损失函数最终形式

加一个负号，将最大化问题将其变为最小化问题：将"爬山"变成"下山"

问题从：
模型该如何调整参数，才能将 似然函数 最大化，从而让"模型对每个样本都预测准确"的可能性最高。

转化为：
模型该如何调整参数，才能将 负的对数似然函数 -∑ᵢ₌₁ⁿ（ yᵢlog(pᵢ) + (1−yᵢ)log(1−pᵢ) ) 最小化？？

因此，将负的对数似然函数定义为 二分类 任务的loss函数，然后用梯度下降法求解最优解，得到模型的最佳参数。

Loss(L) = -∑ᵢ₌₁ⁿ（ yᵢlog(pᵢ) + (1−yᵢ)log(1−pᵢ) )

> Slide 21 · 小结 第 2 章

1 逻辑回归原理

思想：解决分类问题，把线性回归的输出作为逻辑回归的输入

2 逻辑回归的损失函数 – 对数似然损失

数学表达式：Loss(L) = -∑ᵢ₌₁ᵐ（ yᵢlog(pᵢ) + (1−yᵢ)log(1−pᵢ) )

解决二分类问题

> Slide 22 · 习题 第 2 章

1 以下关于逻辑回归的说法正确的是？（多选）

A）逻辑回归应用在分类场景中
B）逻辑回归使用了回归将特征数据进行拟合
C）逻辑回归使用了sigmoid激活函数将回归的结果映射到了(0, 1)值域中
D）逻辑回归的损失函数使用了极大似然估计，因为其输出值是一个概率

答案：ABCD

### 笔记

> 【理解】原理

**Logistic Regression**：分类模型。把线性回归的输出 $z = wx + b$ 喂给 sigmoid，得到 $(0, 1)$ 区间的概率值。

**两步走**：

1. 线性模型按特征重要性算一个分 $z = wx + b$
2. sigmoid 把 $z$ 压到 $(0,1)$ 当概率；阈值（默认 0.5）切成 0/1 两类

**假设函数**：

$$h_w(x) = \sigma(w^\top x + b) = \frac{1}{1 + e^{-(w^\top x + b)}}$$

**Sigmoid**：

$$\sigma(z) = \frac{1}{1 + e^{-z}}$$

性质：$z \to +\infty$ 趋近 1；$z \to -\infty$ 趋近 0；$z = 0$ 时为 0.5。所以"线性回归 + 阈值切分" = 二分类。

**决策**：$h_w(x) \geq 0.5 \Rightarrow \hat{y} = 1$，否则 $\hat{y} = 0$。等价于 $w^\top x + b \geq 0$，即决策边界是 $w^\top x + b = 0$（线性超平面）。

> PPT 原话（逻辑回归原理 – 概念，slide 14）：
>
> 逻辑回归概念 Logistic Regression
> sigmoid函数
> 一种二分类模型，把线性回归的输出，作为逻辑回归的输入。
> 利用sigmoid 函数 ， 转化为（0, 1）之间的概率值
> 基本思想    目的：  二分类
> 逻辑回归的假设函数
> 线性回归的输出，作为逻辑回归的输入，得到分类概率
>
> 〔公式图：图片11.jpg —— 假设函数〕
>
> 讲师备注：数学上表达什么样的？

> PPT 原话（三部曲 + 预测过程例子，slide 15）：
>
> 三部曲：
>
> 1    预测值y (logits)  =
>
> 2    分类概率(归一) = sigmoid( logits )
>
> 3    设定阈值，判断二分类结果
>
> 〔图：图片15.jpg〕〔图：图片8.jpg〕
>
> 举个例子：逻辑回归预测过程
> sigmoid函数
> （比如这里的阈值 (分界线) 为0.6）
>
> 〔图：图片6.jpg〕
> 预测值/得分
> 特征1  特征2   特征3
> 概率（归一）
> 线性模型
> 样本1
> 样本2
> 样本3
> 样本4
> 样本5
>
> 〔图：图片21.jpg〕〔图：图片25.jpg〕
> 判断阈值=0.6
>
> 用什么损失函数 ？
>
> 讲师备注：PPT里面的概率计算 有些问题

**代码 API**：

```python
from sklearn.linear_model import LogisticRegression
clf = LogisticRegression()
clf.fit(X_train, y_train)
clf.predict(X_test)        # 返回 0/1 标签（阈值 0.5）
clf.predict_proba(X_test)  # 返回 [P(y=0), P(y=1)] 概率
```

> 【知道】损失函数

**问题**：MSE 用在分类上，loss 曲面非凸（多个局部最小值），梯度下降会卡住。需要换损失。

**对数似然损失**（log loss / binary cross-entropy）——单样本：

$$\mathrm{loss}(h_w(x), y) = \begin{cases} -\log(h_w(x)) & y = 1 \\ -\log(1 - h_w(x)) & y = 0 \end{cases}$$

**直觉**：真实标签是 1 时，预测概率越接近 1 损失越小；接近 0 损失趋于 $+\infty$。预测错了被无情放大。

**合并写法**（用 $y \in \{0, 1\}$ 当开关）：

$$\mathrm{loss}(h_w(x), y) = -y \log(h_w(x)) - (1 - y) \log(1 - h_w(x))$$

**整体损失**（$m$ 个样本求和/平均）：

$$J(w) = -\frac{1}{m} \sum_{i=1}^{m} \left[ y_i \log(h_w(x_i)) + (1 - y_i) \log(1 - h_w(x_i)) \right]$$

**为什么这个损失好**：与 sigmoid 配合后 $J(w)$ 是凸函数，梯度下降稳定收敛到全局最优。

**代码 API**：

```python
from sklearn.metrics import log_loss
log_loss(y_true, y_pred_proba)  # y_pred_proba 是 predict_proba 的输出
```

> PPT 原话（损失函数手工计算示例，slide 16）：
>
> 〔图：图片4.jpg —— 图示〕
> 累计每个样本的二分类交叉熵loss
> 逻辑回归原理 - 损失函数
>
> 二分类交叉熵
> 损失函数：
> 损失函数的工作原理：每个样本预测值只有两个类别时，预测的（归一）概率应当 与 标签 靠拢
> 用0、1 表示A/B两个类别
>
> 〔图：图片12.jpg —— 手机屏幕截图〕
> 举个例子 –
>     损失函数手工计算
> 难例
> 难例
> 难例
> 难例
> 易例
> -> A
> -> B
> -> A
> -> B
> -> A
>
> 〔公式图：图片16.jpg〕〔公式图：图片10.jpg〕
>
> 计算损失：=- [    1log0.4 + (1-1)log(1-0.4)  +            # 第1个样本产生的 二分类  损失值
>                           0log0.6 + (1-0)log(1-0.68)  +            # 第2个样本产生的 二分类  损失值
>                           1log0.41 + (1-1)log(1-0.41)  +  ….
>
> 讲师备注：线性回归  是累计每个样本的预测误差   逻辑回归 是 累计 每个样本的预测概率的误差。算式中 0.6 与 0.68 不一致疑似 typo，保留原文。

> 【推导】通过极大似然法推导二分类交叉熵损失函数（PPT slide 17）

通过极大似然法     推导二分类交叉熵损失函数

1、1个样本的概率表示

假设：有 0、1 两个类别，某个样本被预测为 1 类的概率为 p, 则预测为 0 类的概率必然为 1-p，
则一个样本分类正确的概率为：
真实值

〔公式图：图片1.jpg〕
等价

〔公式图：图片5.jpg〕

2、n个样本的概率表示

〔公式图：图片18.jpg〕
〔公式图：图片12.jpg〕

pi 表示每个样本单独被分类正确时的概率， 比如 将样本x1准确预测为y1类别的概率是p1，以此类推
yi 表示每个样本的真实类别（0或1）

极大似然法： 即：模型的参数如何调整，才能使得联合概率P 最大 ？  即对"每个样本的类别都预测准确" 的可能性最高 ？

公式（图片公式按文字推断）：

单样本：$P = p^{y}(1-p)^{1-y}$

n 样本联合：$P = \prod_{i=1}^{n} p_i^{y_i}(1-p_i)^{1-y_i}$

> 讲师备注：比如预测明天是否下雨  40%     不下雨就是60%；当y就属于1类时，则 正确率就是p      当y就属于0类时，则 正确率就是1-p；既成事实

> 【推导】似然函数求极大（PPT slide 18）

极大似然法： 即：模型的参数如何调整，才能使得联合概率P 最大 ？  即对"每个样本的类别都预测准确" 的可能性最高 ？

〔公式图：图片5.jpg〕〔公式图：图片4.jpg〕
方法1   似然函数                                                    的导数为0， 求极大值对应的参数

                                                                                       难以计算

〔公式图：图片8.jpg〕

> 【推导】2 梯度下降法（PPT slide 19）

2  梯度下降法

log是单调函数

为了方便计算，将似然函数 转 对数似然函数，取log优化函数：连乘形式转换为对数加法形式：

2   梯度下降法只能求函数最小值问题，如何求解 似然函数的极大值 ？？

〔公式图：图片8.jpg〕
等号2边取log

〔公式图：图片21.jpg〕〔公式图：图片22.jpg〕
 倒反天罡

〔公式图：图片24.jpg〕

> 讲师备注：强调  log是单调函数。"倒反天罡"为讲师文案，指 max → 加负号变 min。

> 【图示】损失函数最终形式（PPT slide 20）

〔公式图：图片12.jpg〕
〔公式图：图片14.jpg —— 图片包含 图示〕

> 备注：纯图示页，无文字。承接 slide 19 推导得到的二分类交叉熵损失最终形式。

> 【小结】第 2 章（PPT slide 21）

1 逻辑回归原理
思想：解决分类问题，把线性回归的输出作为逻辑回归的输入
2 逻辑回归的损失函数– 对数似然损失

> 【习题】第 2 章（PPT slide 22）

1 以下关于逻辑回归的说法正确的是？（多选）
A）逻辑回归应用在分类场景中

B）逻辑回归使用了回归将特征数据进行拟合

C）逻辑回归使用了sigmoid激活函数将回归的结果映射到了(0, 1)值域中

D）逻辑回归的损失函数使用了极大似然估计，因为其输出值是一个概率
答案：ABCD
