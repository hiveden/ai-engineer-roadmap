# 第 4 章 c · ROC 曲线 + AUC 指标

> 维度：**数学 + 代码**
>
> **按知识点拆分的讲解版**：
>
> 1. [`01-roc曲线.md`](./01-roc曲线.md) — 【知道】
> 2. [`02-绘制roc.md`](./02-绘制roc.md) — 【实操】
> 3. [`03-auc.md`](./03-auc.md) — 【知道】AUC 值
> 4. [`04-分类报告api.md`](./04-分类报告api.md) — 【代码】
> 5. [`05-auc计算api.md`](./05-auc计算api.md) — 【代码】

## ━━━━━━━━ 底稿 ━━━━━━━━

### PPT

> 从 [`../04逻辑回归.pptx`](../04逻辑回归.pptx) 提取（slide 47-59）。图占位标 〔图〕；排版整理；文字保留 PPT 原话。

> Slide 47 · 目录（章间导航）

- 逻辑回归简介（应用场景，数学知识）
- 逻辑回归原理
- 逻辑回归API函数和案例
- 分类问题评估（混淆矩阵、精确率、召回率、F1-score、AUC指标、ROC曲线）
- 电信客户流失预测案例

> Slide 48 · 学习目标

- 知道ROC曲线和AUC指标 — 了解
- 了解相应的API函数

> Slide 49 · 分类模型评估方法 – ROC曲线、AUC指标（TPR/FPR 概念）

分类模型评估方法 – ROC曲线、AUC指标 — 了解

真正率TPR与伪正率FPR

1 正样本中被预测为正样本的概率 TPR（True Positive Rate）
2 负样本中被预测为正样本的概率 FPR（False Positive Rate）

通过这两个指标可以描述模型对正/负样本的分辨能力

真正率即召回率：TPR = TP / (TP + FN)

伪正率：FPR = FP / (FP + TN)

概念

〔图：TPR/FPR 定义表格〕

> Slide 50 · 分类模型评估方法 – ROC曲线、AUC指标（ROC 曲线定义）

分类模型评估方法 – ROC曲线、AUC指标

ROC曲线（Receiver Operating Characteristic curve）

受试者工作特征曲线

ROC曲线以模型的真正率TPR为纵轴，假正率FPR为横轴，
它将模型在不同阈值下的表现以曲线的形式展现出来。（比如性别分类--按头发短阈值）

阈值：模型的分类阈值
当阈值发生变化时，模型预测结果发生变化，混淆矩阵的值发生变化。

概率 ≥ 阈值 → 预测为正类（女生）               类比：头发长于多少CM是女生
概率 < 阈值 → 预测为负类                                 箭头方向，阈值宽松

阈值宽松

N个
M个

AUC（Area Under the ROC Curve）曲线下面积

ROC曲线的优劣可以通过曲线下的面积（AUC）来衡量，AUC越大表示分类器性能越好。
当AUC=0.5时，表示分类器的性能等同于随机猜测
当AUC=1时，表示分类器的性能完美，能够完全正确地将正负例分类。

真正率即召回率：TPR = TP / (TP + FN)
伪正率：FPR = FP / (FP + TN)

```
ROC 曲线 ASCII 示意

  TPR
  1.0 ┤    ╭────── 完美分类器（贴左上）
      │   ╭╯
  0.5 ┤  ╭╯     ─── 随机猜测（对角线）
      │ ╭╯  ╌╌╌
  0.0 ┼╌╌╌╌╌╌╌╌╌╌╌╌
     0.0    0.5    1.0   FPR
```

> **Notes**（讲师）：讲清楚 1 横纵坐标  2 曲线含义（不同阈值下，类比头发长度），宽松的含义  3（0,1）坐标的含义  4 对角线 随机猜测  5 面积的含义  6 如果比随机猜测还糟糕，则反过来理解。
> 比如以头发长短判断性别，阈值变化
> AUC=0.5 是随机猜测。比如没有分数和阈值2个概念，对于N个样本，直接随机概率p=0.5预测为正例，那么N个正例中，真正率=0.5N/N；M个假例中，伪正率=0.5M/M，结果横纵坐标都是0.5。
> 现在调整概率阈值p，无论P是多少，横纵坐标相等，且都等于P。
> AUC=1是完美分类器，（0,1）点代表纵坐标分母FN=0，横坐标分子FP=0

> Slide 51 · 分类评估方法 – ROC曲线、AUC指标（4 个特殊点）

分类评估方法 – ROC曲线、AUC指标

ROC 曲线图像中，4 个特殊点的含义

点坐标说明：图像x轴FPR/y轴TPR, 任意一点坐标A(FPR值, TPR值)

点(0, 0)：所有的负样本都预测正确，所有的正样本都预测为错误。相当于点的(FPR值0, TPR值0）
点(1, 0)：所有的负样本都预测错误，所有的正样本都预测错误。相当于点的(FPR值1, TPR值0)
   - 最不好的效果
3.点(1, 1)：所有的负样本都预测错误，表示所有的正样本都预测正确。相当于点的(FPR值1，TPR值1)
4.点(0, 1)：所有的负样本都预测正确，表示所有的正样本都预测正确。相当于点的(FPR值0，TPR值1)
- 最好的效果

从图像上来看：
曲线越靠近(0,1)点则模型对正、负样本的辨别能力就越强

AUC 是 ROC 曲线下面的面积，该值越大，则模型的辨别能力就越强，AUC 范围在 [0, 1] 之间
当 AUC= 1 时，该模型被认为是完美的分类器，但是几乎不存在完美分类器
当 AUC ~ 0.5 时，模型区分正负样本的就会变得模棱两可，近似于随机猜测

伪正率：FPR = FP / (FP + TN)
真正率即召回率：TPR = TP / (TP + FN)

> 备注：编号原文不连续：1、2、3、4 → 实际为"点(0,0)/点(1,0)/3.点(1,1)/4.点(0,1)"，保留原文。

> Slide 52 · 分类评估方法 – ROC曲线、AUC指标（案例题面）

分类评估方法 – ROC曲线、AUC指标

案例：ROC 曲线的绘制

已知：在网页某个位置有一个广告图片，该广告共被展示了 6 次；有 2 次被浏览者点击了。每次点击的概率见图1。
其中正样本{1, 3}  负样本为{2, 4, 5, 6}

要求画出：在不同阈值下的ROC曲线。

图1：每次点击的概率图
图2：根据预测点击概率排序之后的图

思路分析：根据不同的阈值，求出TPR、FPR，得出点坐标画ROC图

模型输出（排序后）：样本 | 标签 | 模型输出
〔图：排序后概率表〕

> Slide 53 · 分类评估方法 – ROC曲线、AUC指标（逐阈值计算）

分类评估方法 – ROC曲线、AUC指标

案例：ROC 曲线的绘制

阈值：0.9（>0.9 才算被检出）
	原本为正例的 1、3 号的样本中 1，3 号样本被分类错误，则 TPR = 0/2 = 0
	原本为负例的 2、4、5、6 号样本没有一个被分为正例，则 FPR = 0

阈值：0.8
	原本为正例的 1、3 号样本中1号样本被分类正确，则 TPR = 1/2 = 0.5
	原本为负例的 2、4、5、6 号样本没有一个被分为正例，则 FPR = 0

阈值：0.7
	原本为正例的 1、3 号样本被分类正确，则 TPR = 2/2 = 1
	原本为负类的 2、4、5、6 号样本均分类正确，则 FPR = 0/4 = 0

阈值：0.6
	原本为正例的 1、3 号样本被分类正确，则 TPR = 2/2 = 1
	原本为负类的 2、4、5、6 号样本中 2号样本被分类错误，则 FPR = 1/4 = 0.25

阈值：0.5
	原本为正例的 1、3 号样本被分类正确，则 TPR = 2/2 = 1
	原本为负类的 2、4、5、6 号样本中 2、4 号样本被分类错误，则 FPR = 2/4 = 0.5

阈值：0.4
	原本为正例的 1、3 号样本被分类正确，则 TPR = 2/2 = 1
	原本为负类的 2、4、5号样本全部被分类错误，则 FPR = 3/4 = 0.75

会得出不同阈值下的点坐标：
(0, 0.5)、(0, 1)、(0.25, 1)、
(0.5, 1)、(0.75, 1)、(1, 1)

由 TPR 和 FPR 构成的 ROC 图像为

ROC曲线上的每个点代表模型在不同阈值下的性能表现

伪正率：FPR = FP / (FP + TN)
真正率即召回率：TPR = TP / (TP + FN)

〔图：ROC 曲线图〕

> 备注：阈值 0.9 段表述"1，3 号样本被分类错误，则 TPR = 0/2 = 0"，按 0.9 阈值确实如此（两个 < 0.9）。点坐标列表与逐阈值结果不完全对应（首点本应是 (0,0)），保留原文。

> Slide 54 · 拓展 – 如何求出最佳阈值

拓展：如何求出最佳阈值？

1 Youden index（约登指数）
核心：从ROC曲线上找到一点，离最佳模型（0,1）点越近，离随机模型（对角线）越远

J = TPR - FPR

或：

F1 score
遍历所有可能的阈值，选择使模型的F1分数最大的阈值作为"最佳阈值"。

〔公式图：约登指数公式〕

> Slide 55 · 分类评估方法 – AUC计算API

分类评估方法 – ROC曲线、AUC指标

AUC的计算api

```python
from sklearn.metrics import roc_auc_score
sklearn.metrics.roc_auc_score(y_true, y_predict)
```

- 计算ROC曲线面积，即AUC值
- y_true：每个样本的真实类别，必须为0(反例),1(正例)标记
- y_predict：预测得分，可以是正例的估计概率、置信值或者分类器方法的返回值

> Slide 56 · 分类评估报告API

分类评估报告api

```python
sklearn.metrics.classification_report(y_true, y_pred, labels=[], target_names=None)
```

- y_true：真实目标值
- y_pred：估计器预测目标值
- labels：指定类别对应的数字
- target_names：目标类别名称
- return：每个类别精确率与召回率

> Slide 57 · 小结 ROC/AUC

1 ROC曲线

ROC曲线以模型的真正率TPR、假正率FPR为y/x轴，它将模型在不同阈值下的表现以曲线的形式展现出来。

2 AUC指标

AUC越大表示分类器性能越好
当AUC=0.5时，表示分类器的性能等同于随机猜测
当AUC=1时，表示分类器的性能完美，能够完全正确地将正负例分类

> Slide 58 · 习题 第 4 章习题 3

1、下列关于逻辑回归模型的评估说法正确的是？（多选）

A）我们在评估逻辑回归模型时只需要选择一种评估方法即可
B）混淆矩阵能够帮助我们快速计算出其它分类模型指标
C）召回率和精确率表达的是同样的概念
D）ROC曲线下与坐标轴形成的闭合区域的面积即为AUC指标的值

答案解析：BD

> Slide 59 · 习题 第 4 章习题 4-5

1 以下哪个描述最准确地解释了AUC指标的含义？

A. AUC是ROC曲线下方的面积，用于比较分类器的性能。
B. AUC是ROC曲线上某一点的斜率，表示分类器的灵敏度。
C. AUC是ROC曲线上的最大误分类率，用于评估分类器的错误率。
D. AUC是ROC曲线上的阈值，用于选择最佳分类器。

答案：A。

2 下面哪个选项最能说明ROC曲线的作用？

A ROC曲线用于描述模型的参数。
B ROC曲线用于可视化模型的损失函数。
C ROC曲线用于评估二元分类模型的性能。
D ROC曲线用于选择最佳的回归模型。

答案：C。

### 笔记

> 【知道】ROC 曲线和 AUC 指标

样本不平衡时，单看 Recall / Precision 易被多数类淹没。ROC + AUC 的核心：**抛开单一阈值，直接评估模型对正负样本的整体辨别能力**。

> ROC 曲线

ROC（Receiver Operating Characteristic）曲线把分类器在**所有阈值**下的表现画成一条线。两个轴分别看正负样本：

| 轴 | 含义 | 公式 |
|---|---|---|
| 纵轴 **TPR**（True Positive Rate） | 正样本中被预测为正的概率 = 召回率 Recall | TP / (TP + FN) |
| 横轴 **FPR**（False Positive Rate） | 负样本中被预测为正的概率 = 误报率 | FP / (FP + TN) |

> PPT 原话（TPR/FPR 概念，slide 49）：
>
> 概念
>
> 〔图：图片1.jpg —— 表格〕
> 真正率TPR与伪正率FPR
>
> 1 正样本中被预测为正样本的概率TPR （True Positive Rate）
>
> 2 负样本中被预测为正样本的概率FPR （False Positive Rate）
>
> 通过这两个指标可以描述模型对正/负样本的分辨能力

> PPT 原话（ROC 曲线 + 阈值类比，slide 50）：
>
> 〔图：图片20.jpg —— 背景图案〕〔图：图片11.jpg —— 表格〕
> N个
>
> M个
>
> ROC曲线（Receiver Operating Characteristic curve）
>
> 受试者工作特征曲线
> ROC曲线以模型的真正率TPR为纵轴，假正率FPR为横轴，
> 它将模型在不同阈值下的表现以曲线的形式展现出来。 （比如性别分类--按头发短阈值）
>
> 〔图：图片5.jpg〕
> 概率 ≥ 阈值 → 预测为正类（女生）               类比：头发长于多少CM是女生
> 概率 < 阈值 → 预测为负类                                 箭头方向，阈值宽松
> 阈值宽松
> 阈值： 模型的分类阈值
>                当阈值发生变化时，模型预测结果发生变化，混淆矩阵的值发生变化。
> AUC (Area Under the ROC Curve）曲线下面积
> ROC曲线的优劣可以通过曲线下的面积（AUC）来衡量，AUC越大表示分类器性能越好。
> 当AUC=0.5时，表示分类器的性能等同于随机猜测
> 当AUC=1时，表示分类器的性能完美，能够完全正确地将正负例分类。
>
> 讲师备注：讲清楚 1  横纵坐标  2 曲线含义（不同阈值下，类比头发长度），宽松的含义   3  （0,1）坐标的含义   4  对角线 随机猜测   5  面积的含义  6  如果比随机猜测还糟糕，则反过来理解
> 比如以头发长短判断性别，阈值变化
> AUC =0.5 是随机猜测。   比如没有分数和阈值2个概念，对于N个样本，直接随机概率p=0.5 预测为正例，
> 那么N 个正例中，  真正率 = 0.5N/N  N 为正例总数。   M个假例中，伪正率 = 0.5M/M，  M 为假例总数， 结果 横纵坐标都是0.5。
> 现在调整  概率阈值p， 无论P是多少，横纵坐标相等，且都等于P。
> AUC=1是完美分类器， （0,1）点 代表  纵坐标分母 FN=0，    横坐标分子 FP=0

> PPT 原话（4 个特殊点，slide 51）：
>
> ROC 曲线图像中，4 个特殊点的含义
>
> 〔图：图片5.jpg〕
> 点坐标说明：图像x轴FPR/y轴TPR, 任意一点坐标A(FPR值,  TPR值)
> 点(0, 0) ：所有的负样本都预测正确，所有的正样本都预测为错误 。相当于点的(FPR值0,  TPR值0）
> 点(1, 0) ：所有的负样本都预测错误，所有的正样本都预测错误。相当于点的(FPR值1,  TPR值0)
>    	- 最不好的效果
> 3.点(1, 1)：所有的负样本都预测错误，表示所有的正样本都预测正确。相当于点的(FPR值1，TPR值1)
> 4.点(0, 1)：所有的负样本都预测正确，表示所有的正样本都预测正确 。相当于点的(FPR值0，TPR值1)
> - 最好的效果
> 从图像上来看
> 曲线越靠近 (0,1) 点则模型对正、负样本的辨别能力就越强
>
> AUC 是 ROC 曲线下面的面积，该值越大，则模型的辨别能力就越强，AUC 范围在 [0, 1] 之间
> 当 AUC= 1 时，该模型被认为是完美的分类器，但是几乎不存在完美分类器
> 当 AUC ~ 0.5 时，模型区分正负样本的就会变得模棱两可，近似于随机猜测
>
> 备注：编号原文不连续：1、2、3、4 → 实际为"点(0,0)/点(1,0)/3.点(1,1)/4.点(0,1)"，保留原文。

**直觉**：TPR 越高 = 抓正样本越准；FPR 越低 = 误伤负样本越少。理想模型 TPR=1 且 FPR=0，即落在 **(0, 1)** 点。

四个特殊点（横轴 FPR，纵轴 TPR）：

| 点 | 含义 |
|---|---|
| (0, 0) | 全预测为负：正样本全错，负样本全对 |
| (1, 1) | 全预测为正：正样本全对，负样本全错 |
| (0, 1) | 完美分类器 |
| (1, 0) | 全反着来（正负全错） |

对角线 y = x 是**随机猜测**基线（TPR = FPR），曲线必须显著高于对角线才算有判别力。

> 绘制 ROC 曲线

**核心机制**：模型输出的是概率，调阈值 → 划分正负 → 算一组 (FPR, TPR) → 连成曲线。

**画法骨架**（4 步）：

1. 模型对每个样本输出**预测概率** `y_score`
2. 按 `y_score` **降序排序**
3. 从高到低**遍历每个分数作为阈值**：≥ 阈值判正，< 阈值判负
4. 每个阈值算一组 (FPR, TPR) → 全部点连线

**广告点击案例**（6 样本，2 正 4 负）：按概率降序遍历，每跨过一个正样本 TPR 跳一格，每跨过一个负样本 FPR 跳一格。最终得到点序列 `(0, 0) → (0, 0.5) → (0, 1) → (0.25, 1) → (0.5, 1) → (0.75, 1) → (1, 1)`，连线就是 ROC。

**几何直觉**：

- 排序后**正样本集中在前** → 曲线先沿纵轴爬到顶 → 形状贴左上角 → 模型好
- 正负样本**交错** → 曲线接近对角线 → 模型烂

> PPT 原话（案例题面 ROC 曲线绘制，slide 52）：
>
> 案例：ROC 曲线的绘制
> 已知：在网页某个位置有一个广告图片，该广告共被展示了 6 次； 有 2 次被浏览者点击了。每次点击的概率见图1。
>            其中正样本{1, 3}  负样本为{2, 4, 5, 6}
> 要求画出：在不同阈值下的ROC曲线。
> 图2：根据预测点击概率排序之后的图
> 图1：每次点击的概率图
> 标签
> 标签
> 模型输出
> 模型输出
>
> 〔图：图片6.jpg —— 概率原表〕
> 〔图：图片3.jpg —— 排序后表〕
> 思路分析：根据不同的阈值，求出TPR、FPR，得出点坐标画ROC图

> PPT 原话（案例逐阈值计算，slide 53）：
>
> 阈值：0.9 （>0.9 才算被检出）
> 	原本为正例的 1、3 号的样本中 1，3 号样本被分类错误，则 TPR = 0/2 = 0
> 	原本为负例的 2、4、5、6 号样本没有一个被分为正例，则 FPR = 0
> 阈值：0.8
> 	原本为正例的 1、3 号样本中1号样本被分类正确，则 TPR = 1/2 = 0.5
> 	原本为负例的 2、4、5、6 号样本没有一个被分为正例，则 FPR = 0
> 阈值：0.7
> 	原本为正例的 1、3 号样本被分类正确，则 TPR = 2/2 = 1
> 	原本为负类的 2、4、5、6 号样本均分类正确，则 FPR = 0/4 = 0
> 阈值：0.6
> 	原本为正例的 1、3 号样本被分类正确，则 TPR = 2/2 = 1
> 	原本为负类的 2、4、5、6 号样本中 2号样本被分类错误，则 FPR = 1/4 = 0.25
> 阈值：0.5
> 	原本为正例的 1、3 号样本被分类正确，则 TPR = 2/2 = 1
> 	原本为负类的 2、4、5、6 号样本中 2、4 号样本被分类错误，则 FPR = 2/4 = 0.5
> 阈值 0.4
> 	原本为正例的 1、3 号样本被分类正确，则 TPR = 2/2 = 1
> 	原本为负类的 2、4、5号样本全部被分类错误，则 FPR = 3/4 = 0.75
> 会得出不同阈值下的点坐标：
> (0, 0.5)、(0, 1)、(0.25, 1)、
> (0.5, 1)、(0.75, 1)、(1, 1)
> 由 TPR 和 FPR 构成的 ROC 图像为
>
> 〔图：图片14.jpg —— ROC 曲线图〕
> ROC曲线上的每个点
> 代表模型在不同阈值下的性能表现
>
> 备注：阈值 0.9 段表述"1，3 号样本被分类错误，则 TPR = 0/2 = 0"，按 0.9 阈值确实如此（两个 < 0.9）。点坐标列表与逐阈值结果不完全对应（首点本应是 (0,0)），保留原文。

> 【拓展】如何求出最佳阈值（PPT slide 54）

拓展 ： 如何求出最佳阈值 ？
F1 score

遍历所有可能的阈值，选择使模型的F1分数最大的阈值作为"最佳阈值"。
1 Youden index （约登指数）
核心: 从ROC曲线上找到一点，离最佳模型（0,1）点越近，
离随机模型（对角线）越远

〔公式图：图片6.jpg〕
〔公式图：图片8.jpg〕
或：
〔公式图：图片10.jpg〕

公式（按约登指数常识推断）：

$J = TPR - FPR$

> AUC 值

**AUC**（Area Under Curve）= ROC 曲线下面积，把"形状好不好"压成一个 **[0, 1]** 数字。

**解读速查**：

| AUC | 含义 |
|---|---|
| 1.0 | 完美分类器（几乎不存在） |
| 0.5 | 随机猜测，模型无效 |
| < 0.5 | 比随机还差，预测反着用即可 |
| 0.7 ~ 0.85 | 工程实践中常见的"可用"区间 |
| > 0.9 | 强判别力（警惕数据泄漏） |

**概率解释**：随机抽一个正样本和一个负样本，AUC = 模型给正样本打分 > 给负样本打分的概率。

**核心定位**：AUC 评估的是**正负样本的辨别能力**，与阈值选择无关 → 适合不平衡场景。

> 分类评估报告 API

一次性给出**每个类别**的 Precision / Recall / F1 / Support，避免手动从混淆矩阵抠数。

```python
sklearn.metrics.classification_report(
    y_true,                # 真实标签
    y_pred,                # 预测标签（不是概率）
    labels=[],             # 指定要展示的类别编号
    target_names=None,     # 类别可读名（替换数字标签）
)
```

**关键点**：

- 输入是**离散预测**（`predict` 的输出），不是概率
- 输出 `macro avg`（各类等权平均）和 `weighted avg`（按 support 加权）
- `target_names` 长度必须与 `labels` 一致
- 不平衡场景重点看**少数类的 Recall**

> AUC 计算 API

```python
from sklearn.metrics import roc_auc_score

sklearn.metrics.roc_auc_score(
    y_true,    # 真实类别，必须是 0/1（反例/正例）
    y_score,   # 预测得分：正例概率 / 置信度 / decision_function 返回值
)
```

**关键点**：

- `y_score` 传**概率或得分**（如 `model.predict_proba(X)[:, 1]`），**不是 `predict` 的 0/1 标签**——传标签会退化为单点 AUC，失去曲线意义
- 标签必须是 0/1；其他取值（如 1/2）需先重映射
- 多分类用 `multi_class='ovr'` / `'ovo'` 参数
- 配套绘图：`sklearn.metrics.roc_curve(y_true, y_score)` → 返回 `fpr, tpr, thresholds`

> PPT 原话（AUC 计算 API，slide 55）：
>
> AUC的计算api
>
> from sklearn.metrics import roc_auc_score
> sklearn.metrics.roc_auc_score(y_true, y_predict)
>  计算ROC曲线面积，即AUC值
>  y_true：每个样本的真实类别，必须为0(反例),1(正例)标记
>  y_predict：预测得分，可以是正例的估计概率、置信值或者分类器方法的返回值

> PPT 原话（分类评估报告 API，slide 56）：
>
> 分类评估报告api
>
> sklearn.metrics.classification_report(y_true, y_pred, labels=[], target_names=None )
> y_true：真实目标值
> y_pred：估计器预测目标值
> labels:指定类别对应的数字
> target_names：目标类别名称
> return：每个类别精确率与召回率

> 【小结】ROC/AUC（PPT slide 57）

1 ROC曲线
ROC曲线以模型的真正率TPR、假正率FPR为y/x轴，它将模型在不同阈值下的表现以曲线的形式展现出来。
2 AUC指标
AUC越大表示分类器性能越好
当AUC=0.5时，表示分类器的性能等同于随机猜测
当AUC=1时，表示分类器的性能完美，能够完全正确地将正负例分类

> 【习题】第 4 章习题 3（PPT slide 58）

1、下列关于逻辑回归模型的评估说法正确的是？（多选）
A）我们在评估逻辑回归模型时只需要选择一种评估方法即可

B）混淆矩阵能够帮助我们快速计算出其它分类模型指标

C）召回率和精确率表达的是同样的概念

D）ROC曲线下与坐标轴形成的闭合区域的面积即为AUC指标的值
答案解析：BD

> 【习题】第 4 章习题 4-5（PPT slide 59）

1 以下哪个描述最准确地解释了AUC指标的含义？
A. AUC是ROC曲线下方的面积，用于比较分类器的性能。
B. AUC是ROC曲线上某一点的斜率，表示分类器的灵敏度。
C. AUC是ROC曲线上的最大误分类率，用于评估分类器的错误率。
D. AUC是ROC曲线上的阈值，用于选择最佳分类器。
答案：A。
2 下面哪个选项最能说明ROC曲线的作用？
A ROC曲线用于描述模型的参数。
B ROC曲线用于可视化模型的损失函数。
C ROC曲线用于评估二元分类模型的性能。
D ROC曲线用于选择最佳的回归模型。
答案：C。

## ━━━━━━━━ Sources ━━━━━━━━

- AUC 解读速查表（0.7 / 0.85 / 0.9 经验阈值）：教学经验补全
- AUC 概率解释（随机抽正负样本得分比较）：Mason & Graham (2002) 经典等价表述
