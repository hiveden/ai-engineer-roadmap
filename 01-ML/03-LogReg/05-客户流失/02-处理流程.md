---
tags: [算法/逻辑回归, 案例/客户流失, 特征/独热编码, 评估/AUC]
---

# 处理流程

> 维度：**代码**
> 知识点级别：【知道】数据处理到模型评估的四步流水线
> 章节底稿全文见 [`README.md`](./README.md)（PPT slide 64）

## ━━━━━━━━ 底稿 ━━━━━━━━

### PPT

> Slide 64 · 案例 – 电信客户流失预测（案例步骤分析）

案例 – 电信客户流失预测

案例步骤分析

1、数据基本处理 / 清洗
主要是查看数据行/列数量
对类别数据数据进行one-hot处理
查看标签分布情况

2、特征筛选
分析哪些特征对标签值影响大
对标签进行分组统计，对比0/1标签分组后的均值  等等
初步筛选出对标签影响比较大的特征，形成x、y

3、模型训练
样本均衡情况下模型训练
样本不平衡情况下模型训练
交叉验证网格搜素等方式模型训练

4、模型评估
精确率
Roc_AUC指标计算

> ⚠ PPT 原文"网格搜素" 疑似 typo（"搜索"），保留原文。

### 笔记

> 【知道】处理流程

1. **数据基本处理**
   - 查看 dtype / 缺失 / 标签分布（流失类通常类别不平衡）
   - 类别特征 one-hot 编码
   - `customerID` 丢弃，`TotalCharges` 字符串转数值
2. **特征筛选**
   - 看每个特征对 `Churn` 的边际分布（透视表 / 相关性）
   - 初筛影响大的特征构造 `X` / `y`
3. **模型训练**
   - LogisticRegression 拟合
   - 交叉验证 + GridSearch 调 `C` / `penalty`
4. **模型评估**
   - Accuracy / Precision / Recall（流失场景看 Recall）
   - ROC / AUC

> PPT 原话（案例步骤分析，slide 64）：
>
> 案例步骤分析
> 1、数据基本处理 / 清洗
> 主要是查看数据行/列数量
> 对类别数据数据进行one-hot处理
> 查看标签分布情况
> 2、特征筛选
> 分析哪些特征对标签值影响大
> 对标签进行分组统计，对比0/1标签分组后的均值  等等
> 初步筛选出对标签影响比较大的特征，形成x、y
> 3、模型训练
> 样本均衡情况下模型训练
> 样本不平衡情况下模型训练
> 交叉验证网格搜素等方式模型训练
> 4、模型评估
> 精确率
> Roc_AUC指标计算
>
> 备注："网格搜素" 疑似 typo（"搜索"），保留原文。

---

## ━━━━━━━━ 讲解 ━━━━━━━━

### 生活锚 · 做菜流水线 5 步走

家里做一顿西红柿炒鸡蛋，你不会从"端上桌"那步开始。流程是固定的 5 段：

1. **采购**：去菜场买西红柿 4 个、鸡蛋 6 个、葱姜蒜——回家先把袋子摊开看看，烂的、撞坏的挑出来扔了
2. **洗切**：西红柿烫皮去蒂切块，葱切花，鸡蛋打散搅匀
3. **翻炒**：先下蛋，半熟盛出；再下蒜末爆香、下西红柿炒出汁、把蛋倒回去合炒
4. **装盘**：起锅装盘，撒葱花
5. **试味**：尝一口看咸淡——淡了补盐、咸了加水煮一下

每一步都不能跳：没洗就切会硌牙，没切就炒会炒糊外面没熟里面，没尝就端上桌客人吃出怪味。原始数据到模型预测的旅程也是这 5 段，跳哪段都翻车。下面把这条流水线对到电信客户案例上。

### 业务问题

运营经理拿到一份 7043 行 × 21 列的原始客户名单 CSV：性别一列、套餐一列、月费一列、是否流失一列……他想直接交给数据团队"训个模型挑高风险用户"。可这份名单原样递过去根本跑不通——性别是"Male / Female"汉字字符串，月费总额这一列里夹着 11 个空格行，第一列还是 7043 个互不相同的客户编号。

**从"原始名单"到"能挑出高风险用户"之间，隔着一段固定的流水线**——清洗、筛特征、训模型、看效果。每一步都是一个"不走这一步就出不了结果 / 走错就把营销名单做歪"的关卡。

把这段流水线画清楚，是把案例从"能跑"做到"能上线"的分水岭。

### 【代码】流程 ASCII

```
   ┌───────────────────────────────────────────────────────────────┐
   │  raw CSV  (7043 × 21, 含字符串 / 伪缺失 / ID 列)               │
   └──────────────────────────┬────────────────────────────────────┘
                              │
                       ① 数据基本处理
                              │
                  ┌───────────┼───────────┐
                  ▼           ▼           ▼
           drop customerID  TotalCharges  Churn
                            字符 → 数值   Yes/No → 1/0
                                          类别列 one-hot
                              │
                              ▼
   ┌───────────────────────────────────────────────────────────────┐
   │  clean DataFrame  (~7032 行, one-hot 后约 30+ 列)              │
   └──────────────────────────┬────────────────────────────────────┘
                              │
                       ② 特征筛选
                              │
            ┌─────────────────┼─────────────────┐
            ▼                 ▼                 ▼
       透视表看分布       相关性矩阵         保留 top-k
       (Contract、         (与 Churn 的       构造 X / y
       tenure)            Pearson 相关)
                              │
                              ▼
   ┌───────────────────────────────────────────────────────────────┐
   │  X (n × k) , y (n,)                                            │
   └──────────────────────────┬────────────────────────────────────┘
                              │
                       ③ 模型训练
                              │
                  ┌───────────┼───────────┐
                  ▼           ▼           ▼
            train_test_   StandardScaler  GridSearchCV
            split          fit on train   (C, penalty)
            stratify=y                    cv=5, scoring=roc_auc
                              │
                              ▼
   ┌───────────────────────────────────────────────────────────────┐
   │  best estimator                                                │
   └──────────────────────────┬────────────────────────────────────┘
                              │
                       ④ 模型评估
                              │
                  ┌───────────┼───────────┐
                  ▼           ▼           ▼
            accuracy        classification   roc_auc_score
            (参考)          _report          (主指标)
                            (Recall on
                             churn=1 关键)
```

### 【代码】各阶段决策点

#### ① 数据基本处理

| 决策点 | 错的做法 | 对的做法 |
|---|---|---|
| `customerID` | 直接 one-hot → 7043 个新列爆内存 | `drop(columns=["customerID"])` |
| `TotalCharges` 是字符串 | `astype(float)` 直接报 ValueError | `pd.to_numeric(..., errors="coerce")` 先转成 NaN，再 dropna |
| 标签 `Churn` Yes/No | 留字符串 → sklearn 报 "unknown label type" | `(df["Churn"] == "Yes").astype(int)`，Yes→1 |
| 类别列 | 不编码 → fit 报错 | `pd.get_dummies(drop_first=True)` |

#### ② 特征筛选

PPT 给的方向是**边际分布 marginal distribution**——按 `Churn` 分组看每个特征的均值/计数差距。

- 强信号特征（PPT slide 67 选用）：`Contract_Month`、`internet_other`、`PaymentElectronic`——这三类用户流失率显著高于均值
- 工程实务里也可以全特征丢给 LR，让 L2 正则把弱特征系数压小（**模型自带特征选择能力**）。两条路都对，PPT 走人工筛选路线，便于讲解

#### ③ 模型训练

- `train_test_split(stratify=y)`：流失只占 26.5%，**分层抽样 stratified sampling** 保证训练 / 测试集流失比例一致，否则测试集可能波动到 22% 或 30%
- `StandardScaler`：`tenure` 范围 0–72，`MonthlyCharges` 范围 18–119，`TotalCharges` 0–8000，量纲差三个数量级，正则化（regularization）项会偏向打压数值大的特征——**先标准化再喂 LR**
- `GridSearchCV(scoring="roc_auc")`：网格搜索（grid search）只找 `C`（正则强度倒数）和 `penalty`（L1/L2）。**评分用 AUC 不用 accuracy**——见下方反例

#### ④ 模型评估

- Accuracy（准确率）：流失场景下天然有 73.5% 的"白送"基线（全判不流失）——**单看 accuracy 会被骗**
- Recall on churn=1（流失类召回率）：业务核心指标，"真要走的人里，模型抓住了几个"
- AUC（ROC 曲线下面积）：模型对正负样本的**排序能力**，不依赖阈值，跨模型可比

> 反例：把 `scoring="accuracy"` 喂给 GridSearchCV，搜出来的最优模型可能是"对所有人都判不流失"——accuracy 73.5%，看着不差，但 Recall on churn=1 = 0，业务侧拿到的留存名单是空的。换成 `scoring="roc_auc"` 才会奖励"把流失用户排到前面"的模型。

具体公式 / ROC 曲线绘制 / 阈值调优**不在本章范围**——主场在 04 评估章。

**这一节的关键启示**：raw 到模型隔着 4 段流水线，每段都有不做就崩的决策。

### 术语

| 故事里的元素 | 术语名 | 主场 |
|---|---|---|
| 把 Yes/No 变 1/0 | 标签编码 label encoding | 本节 |
| 把字符串特征变多列 0/1 | 独热编码 one-hot encoding | 03-案例实现 |
| 切分时按 y 比例 | 分层抽样 stratified sampling | 本节 |
| 量纲拉齐 | 标准化 standardization / `StandardScaler` | KNN 04 主场 |
| 自动试参 | 网格搜索 grid search / `GridSearchCV` | 02-LR 05 章 |
| 流失场景的核心指标 | 召回率 recall / AUC | 04 评估章 |

→ 下一步：[`03-案例实现.md`](./03-案例实现.md)

> Sources：
> - PPT Slide 64
