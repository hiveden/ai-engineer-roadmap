# 第 1 章 · 决策树简介

> 维度：**概念**
>
> **按知识点拆分的讲解版**：
>
> 1. [`01-例子.md`](01-决策树概述.md) — 【理解】决策树例子（相亲场景的 if-else 决策）
> 2. [`02-建树流程.md`](./02-建树流程.md) — 【知道】决策树是什么 / 三步建立流程

## ━━━━━━━━ 底稿 ━━━━━━━━

### PPT

> 从 [`../05决策树.pptx`](../05决策树.pptx) 提取（Slide 1-9）。图占位标 〔图〕；排版整理；文字保留 PPT 原话。

#### Slide 1-2 · 章扉页 + 章节索引

**决策树章节索引**：

- 决策树简介
- ID3 决策树
- C4.5 决策树
- CART 决策树
- 案例：泰坦尼克号生存预测
- CART 回归树
- 决策树剪枝

#### Slide 3 · 学习目标（决策树简介章）

- 理解决策树算法的基本思想
- 知道构建决策树的步骤

#### Slide 4 · 决策树简介 – 生活中的决策树

| 故事元素 | 术语 |
|---|---|
| 相亲对象 | 样本 |
| 依靠的信息 | 特征 |

对每个样本预测一个二分类的类别："见" / "不见"

**与逻辑回归的不同点 ？？**

女孩相亲的决策树：

```
                    年龄 ≤ 30 ?
                  ┌─────┴─────┐
                 是           否
                  │            │
              长相帅 ?       不见
              ┌──┴──┐
             是    否
              │     │
          收入高?  不见
          ┌──┴──┐
         是    否
          │     │
         见  公务员?
              ┌──┴──┐
             是    否
              │     │
             见    不见
```

每个内部节点 = 特征判断；每条分支 = 判断结果；每片叶子 = "见 / 不见"。

- **推理过程**：对测试样本从根节点一路走到叶子，打上标签
- **训练过程**：从数据里学出"先看哪个特征 / 阈值切在哪 / 叶子打什么标签" —— 特征选择、顺序、分割点

> **Notes**：解决分类和回归问题；给每个男青年打上标签"见还是不见"。
> 有的选择不同的特征分界，有的选择不同的特征顺序，有的干脆用其他特征。
> 逻辑回归是所有特征一起输入，进行分类得分预测；但是这里是特征被依次使用。于是就讲究特征的使用顺序以及分割点。

#### Slide 5 · 决策树简介

决策树是一种树形结构：**3 个模块**

- 树中每个内部节点表示一个用于判断的特征
- 每个分支代表一个判断结果的输出（≤30 or >30）
- 每个叶子节点代表一种分类结果（对每个样本预测标签）

```
        ┌──────────────┐
        │   年龄 ≤ 30  │ ← ① 内部节点（特征判断）
        └──────┬───────┘
       ≤30 ┌───┴───┐ >30
           │       │      ← ② 分支（判断结果输出）
           ▼       ▼
        ┌─────┐ ┌─────┐
        │ 见  │ │不见 │   ← ③ 叶子节点（分类结果）
        └─────┘ └─────┘
```

#### Slide 6 · 决策树（分类）的目标

是希望通过**一系列最合适的特征**、**合适的特征顺序**、**最合适的分割点**将样本进行分类，使得分割后的子类中，样本尽可能单一，减少混杂。从而便于决策。

**决策树执行结果**：标签 / 标签 / 标签 / 标签

```
切分前（混杂）              切分后（尽可能单一）
─────────────              ─────────────────────
                            子类 A：████████ 见
█▓█▓█▓█▓█▓█▓                子类 B：████████ 不见
见 / 不见 混在一起           子类 C：████████ 见
                            子类 D：████████ 不见
H 大（难决策）               H 小（便于决策）
```

剩余样本尽可能单一，减少混杂（避免误认和漏认）。因此特征以及分割点需要权衡。

#### Slide 7 · （PPT 配图：纯装饰，已略）

#### Slide 8 · 核心需要决定

决策树的目标：是希望通过一系列最合适的特征、合适的特征顺序、最合适的分割点将样本进行分类，使得分割后的子类中，样本尽可能单一，减少混杂。从而便于决策。

```
   ① 选特征      →    ② 定分割点    →    ③ 剪枝
─────────────       ─────────────       ─────────────
哪个特征切下去      切在哪个值上         砍掉过深 / 过细
让子集最纯？        把样本切两半？       的分支防过拟合
（年龄?长相?）      （年龄 ≤ 30?）       （预剪枝/后剪枝）
   ▼                   ▼                   ▼
信息增益 / 增益率 /   遍历候选阈值        泛化能力 ↑
基尼指数            选最优               训练误差 ↑ 一点
```

**核心需要决定**：

1. 选择什么特征进行分割
2. 确定最佳分割点
3. 通过剪枝防止过拟合

#### Slide 9 · 思考题

```
相亲特征优先级（直觉排序）：

  根 ─→ 年龄 ─→ 长相 ─→ 收入 ─→ 是否公务员
   重要性高 ━━━━━━━━━━━━━━━━━━━━━━━━━━━ 重要性低

为什么这个顺序？
  年龄一刀就能砍掉一大批 → 切完最"纯"
  公务员只是锦上添花 → 切完区分度小

ML 视角：哪个特征切完子集最纯（不确定性下降最多）→ 优先选它
        ↓
   下一章：用"信息增益"把"纯"量化出来
```

1. 女孩相亲过程中，更看重哪些特征呢？
   - 年龄 > 长相 > 收入 > 是否公务员
2. 机器学习算法中，选择数据集中的哪些特征进行分裂，会更好呢？

### 笔记

> 01 · 决策树简介

**学习目标**：

1. 理解决策树算法的基本思想
2. 知道构建决策树的步骤

> 【理解】决策树例子

决策树算法是一种监督学习算法，英文是 Decision tree。

决策树思想的来源非常朴素，试想每个人的大脑都有类似于 if-else 这样的逻辑判断，这其中的 if 表示的是条件，if 之后的 else 就是一种选择或决策。程序设计中的条件分支结构就是 if-else 结构，最早的决策树就是利用这类结构分割数据的一种分类学习方法。

比如：你母亲要给你介绍男朋友，是这么来对话的：

> 女儿：多大年纪了？
>
> 母亲：26。
>
> 女儿：长的帅不帅？
>
> 母亲：挺帅的。
>
> 女儿：收入高不？
>
> 母亲：不算很高，中等情况。
>
> 女儿：是公务员不？
>
> 母亲：是，在税务局上班呢。
>
> 女儿：那好，我去见见。

于是你在脑袋里面就有了下面这张图（〔图〕images/01.png）。

作为女孩的你在决策过程就是典型的分类树决策。相当于通过年龄、长相、收入和是否公务员对将男人分为两个类别：见和不见。

> 【知道】决策树简介

**决策树是什么？**

决策树是一种树形结构，树中每个内部节点表示一个特征上的判断，每个分支代表一个判断结果的输出，每个叶子节点代表一种分类结果。

**决策树的建立过程**：

1. 特征选择：选取有较强分类能力的特征。
2. 决策树生成：根据选择的特征生成决策树。
3. 决策树也易过拟合，采用剪枝的方法缓解过拟合。
