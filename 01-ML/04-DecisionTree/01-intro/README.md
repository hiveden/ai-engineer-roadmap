# 第 1 章 · 决策树简介

> 维度：**概念**
>
> **按知识点拆分的讲解版**（待补）：
>
> 1. `01-例子.md` — 【理解】决策树例子（相亲场景的 if-else 决策）
> 2. `02-简介.md` — 【知道】决策树是什么 / 三步建立流程

## 底稿

> 01 · 决策树简介

**学习目标**：

1. 理解决策树算法的基本思想
2. 知道构建决策树的步骤

> 【理解】决策树例子

**Decision Tree**：监督学习算法。本质就是工程师每天写的 `if-else` 条件分支结构——只是树是从数据里"学"出来的，不是手写的。

**经典例子**：相亲。"年龄 → 长相 → 收入 → 是否公务员" 一路问下来分到"见 / 不见"两类。每个内部节点是一次特征判断，每条分支是判断结果，每个叶子是最终决策。

**与 KNN/LR 的差异**：

- KNN：懒惰学习，预测时才算距离
- LogReg：线性决策边界
- Decision Tree：训练阶段切分特征空间得到一组**轴对齐的矩形区域**；可解释性强，无需标准化

> 【知道】决策树简介

**结构定义**：

- **内部节点**：一个特征上的判断条件
- **分支**：判断结果的输出
- **叶子节点**：一种分类（或回归）结果

**建立过程三步走**：

1. **特征选择**：选取分类能力强的特征（按某种准则，如信息增益 / 信息增益率 / 基尼指数）
2. **决策树生成**：递归用选出的特征切分数据
3. **剪枝**：决策树极易过拟合，用预剪枝 / 后剪枝缓解

**算法族对比预览**（详见后续章节）：

| 算法 | 准则 | 输出 |
|---|---|---|
| ID3 | 信息增益 | 多叉树，仅离散特征 |
| C4.5 | 信息增益率 | 多叉树，支持连续 + 缺失值 |
| CART | 基尼指数 / 平方误差 | 二叉树，分类 + 回归 |

**工程视角**：

- 训练产物是一棵树（节点 + 阈值），可序列化为 JSON / PMML / ONNX
- 推理 = 从根走到叶，复杂度 $O(\log n)$（平衡时）
- 天然处理混合类型特征，无需 one-hot / 标准化
