---
tags: [算法/集成学习/GBDT, 数学/负梯度, 数学/损失函数, 数学/梯度下降, 算法/提升树]
---

# 梯度提升树（GBDT）

> 维度：**算法 + 数学**
> 知识点级别：【理解】负梯度拟合（GBDT 核心）
> 章节底稿全文见 [`README.md`](./README.md)（PPT Slide 54-55, 62, 补充 Slide 13-14）

## ━━━━━━━━ 底稿 ━━━━━━━━

### PPT

> Slide 54 · 梯度提升树（GBDT）

**GBDT（Gradient Boosting Decision Tree）**：利用梯度下降的近似方法，利用损失函数的负梯度作为提升树算法中的残差近似值。

**假设**：
- 前一轮迭代得到的强学习器是：$f_{i-1}(x)$
- 损失函数为平方损失：$L(y, f_{i-1}(x))$
- 本轮迭代的目标是找到一个弱学习器：输出预测值 $h_i(x)$
- 让本轮的损失最小化：$\text{Loss} = L(y, f_{i-1}(x) + h_i(x))$
- 则要拟合单个样本的负梯度为：

$$r_i = -\left[\frac{\partial L(y, f(x))}{\partial f(x)}\right]_{f = f_{i-1}(x)}$$

即：对于平方损失，**GBDT 拟合的负梯度就是残差**。

```
对于任一样本（负梯度 = 残差，平方损失情形）：
  r_i = y_i - f_{i-1}(x_i)

对于任一模型参数：
  (梯度下降方向 = 负梯度方向)
```

> Slide 55 · 梯度提升树——基础学习器（初始化）

**对于整个训练集：给出标签的均值，作为基准值。**

执行标准：当模型预测值为何值时，会使得第一个弱学习器在所有训练样本上预测的平方误差最小，即：求损失函数对 $f(x_i)$ 的导数，并令导数为 0。

$$L(y, f(x)) = \frac{1}{2}(y - f(x))^2 \Rightarrow \text{求平方误差最小}$$

$$\frac{\partial}{\partial f(x_i)} \sum_{i} L(y_i, f(x_i)) = 0$$

$$\Rightarrow f(x) = \frac{1}{n}\sum_{i=1}^{n} y_i = \bar{y}$$

从公式可得：初始化树的输出值为目标值的均值时，可使得第一个弱学习器的平方误差最小。

```
商品总价 82 元类比：
  初始猜测 = 均值（50+20+10 周围某个值）
  作为一个基准值，不能差太多！！
```

> Slide 62 · 梯度提升树的构建流程

1. 初始化弱学习器（目标值的均值作为预测值）
2. 迭代构建学习器，每一个学习器拟合上一个学习器的负梯度
3. 直到达到指定的学习器个数
4. 当输入未知样本时，将所有弱学习器的输出结果组合起来作为强学习器的输出

> Slide 13（补充）· GBDT 本质——为什么是负梯度

在 GBDT 中，每棵树预测的是前一棵树的预测值的残差

**为什么是预测残差？**

**GBDT 本质**：

```
所以 GBDT 回归树的预测实际上是负梯度
注意学习率（shrinkage）
```

> Slide 14（补充）· 负梯度计算

在 GBDT 中，每棵树预测的是前一棵树的预测值的残差

**为什么是预测残差？**

**负梯度怎么计算？**

```
所以 GBDT 回归树的预测实际上是负梯度
（平方损失下：负梯度 = 残差 = y - f(x)）
```

### 笔记

> 【理解】梯度提升树

**核心改进**：不直接拟合残差，而是拟合**损失函数对当前模型的负梯度**（最速下降近似）。

**形式化**：

- 前一轮强学习器：$f_{i-1}(x)$
- 损失函数：$L(y, f_{i-1}(x))$
- 本轮目标：找弱学习器 $h_i(x)$ 使 $L(y, f_{i-1}(x) + h_i(x))$ 最小

**负梯度**作为本轮拟合目标：

$$r_i = -\left[\frac{\partial L(y, f(x))}{\partial f(x)}\right]_{f = f_{i-1}}$$

**平方损失情形**：$L = \frac{1}{2}(y - f(x))^2$，则 $-\frac{\partial L}{\partial f(x)} = y - f(x)$，即负梯度 $r_i = y_i - f_{i-1}(x_i) =$ 残差。所以平方损失下 GBDT 退化为提升树。

**分类情形**：损失换成 logloss，拟合的就是 logloss 的负梯度（不再是残差）。

---

## ━━━━━━━━ 讲解 ━━━━━━━━

### 生活锚 · 蒙眼下山找最低点

**业务痛点**：上一节提升树用残差接力很顺手，但**它只在平方损失下成立**。一旦换成分类（对数损失）或抗噪回归（MAE / Huber），"残差"这个概念要么没了、要么含义不对——同样的接力机制就跑不动。

**生活锚（沿用 LR 章蒙眼下山）**：山顶蒙眼下山，每一步靠脚底感受**最陡的下坡方向**（梯度），朝相反方向迈一步。这个"最陡下坡方向"就叫**负梯度**（negative gradient）——**不管山是什么形状（平方坑 / 绝对值坑 / log 坑），脚底都能告诉你往哪迈最快**。

> 平方损失这座山很特殊：它的"最陡下坡方向"恰好等于"还差多少"——也就是残差。换一座山形（换损失函数），方向还在，只是不再叫残差。

一句话：**提升树拟合的是"残差"，GBDT 拟合的是"负梯度"——前者是后者在平方损失下的特例**。下面把这个特例关系拆开讲。

### 业务问题

把场景对照一下就清楚问题在哪：

```
场景：预测房价（回归 · 平方损失）
  L = ½(y - f)²
  → "差多少"有干净的物理含义（差 5 万、差 10 万）
  → 下棵树拟合差额，直接对得上

场景：泰坦尼克（二分类 · 对数损失）
  L = -y·log(p) - (1-y)·log(1-p)
  → 真实 y∈{0,1},预测 p∈(0,1)
  → "y - p" 还能算，但它是"概率差"不是"几何差"
  → 用这个去拟合新树,还能解释通吗？
  
场景：含离群点的回归（MAE 损失）
  L = |y - f|
  → 差额本身存在,但损失曲线不光滑
  → "差额"和"损失下降最快的方向"对不上了
```

提升树的接力机制依赖一个核心**等式**：「下一棵树的目标 = 当前损失下降最快的方向」。平方损失下这个等式恰好简化成"目标 = 残差"，但其他损失函数下这两件事不重合——硬塞残差，下一棵树就不再保证"让总损失变小"。

**GBDT 的解法**：把**损失函数的负梯度**作为通用接力对象，称为**伪残差**（pseudo-residual）。不管什么损失函数，每轮都能算出一个"往哪修、修多少"的方向信号，下一棵树照着这个信号学。下面看数学。

### 【数学】负梯度 · 把"残差"推广

#### 数学符号扫盲

| 符号 | 读法 | 直觉 | 例子 |
|---|---|---|---|
| $L(y, f(x))$ | L of y and f | 损失函数：真实值 $y$ 与预测 $f(x)$ 的"差距度量" | $\frac{1}{2}(y-f)^2$ |
| $\frac{\partial L}{\partial f(x)}$ | partial L over partial f | L 对 f 的偏导；预测稍微变一点，损失怎么变 | $-(y - f)$（平方损失） |
| $-\frac{\partial L}{\partial f(x)}$ | 负梯度（negative gradient） | 损失下降最快的方向 | $y - f$（平方损失 = 残差） |
| $\tilde{y}_i$ / $r_{mi}$ | r sub m i | 第 m 轮第 i 个样本的伪残差 | $y_i - f_{m-1}(x_i)$ |
| $\eta$ | eta / 学习率 | 每棵树贡献打折，防过拟合 | 0.1 |
| $f_{m-1}(x)$ | f sub m-1 | 前 m-1 棵树的累积预测值 | 前 2 轮预测之和 |

#### 核心推导：负梯度在平方损失下 = 残差

平方损失（带 1/2 是为了求导后消掉系数）：

$$L(y, f(x)) = \frac{1}{2}(y - f(x))^2$$

对预测值 $f(x)$ 求偏导：

$$\frac{\partial L}{\partial f(x)} = \frac{\partial}{\partial f(x)} \frac{1}{2}(y - f(x))^2 = -(y - f(x))$$

加负号（取负梯度）：

$$-\frac{\partial L}{\partial f(x)} = y - f(x) = \text{残差}$$

**结论**：平方损失下，负梯度 = 残差。提升树是 GBDT 的特殊情形（平方损失版本）。

```
GBDT（通用）
    ↓  当 L = 平方损失时
提升树（Boosting Tree）
```

#### 不同损失函数下的"伪残差"

| 损失函数 | 场景 | $L(y, f)$ | 负梯度 $\tilde{y}_i$ |
|---|---|---|---|
| 平方损失 | 回归（连续值） | $\frac{1}{2}(y-f)^2$ | $y - f$（就是残差） |
| 绝对损失（MAE） | 回归（抗离群点） | $\lvert y-f \rvert$ | $\text{sign}(y-f)$（只有 +1/-1，方向信号） |
| 对数损失（logloss） | 二分类 | $-y\log(p)-(1-y)\log(1-p)$ | $y - p$（真实标签 − 预测概率） |

每种损失的负梯度都有直觉：**损失函数对当前预测说"差了多少、往哪修"，负梯度就是这个方向信号**。

#### 梯度提升算法（5 步伪代码）

```
GBDT 算法

输入：训练集 {(x₁,y₁), ..., (xₙ,yₙ)}
      损失函数 L(y, f(x))
      弱学习器数量 T，学习率 η

──────────────────────────────────────────────

Step 0 · 初始化（找让所有样本平均损失最小的常数）

    f₀(x) = argmin_c Σᵢ L(yᵢ, c)

    平方损失下：令导数为 0 → c* = ȳ（均值）

──────────────────────────────────────────────

for m = 1, 2, ..., T:

    Step 1 · 计算伪残差（负梯度）

        r̃ᵢₘ = -[ ∂L(yᵢ, f(xᵢ)) / ∂f(xᵢ) ]  f=f_{m-1}

        平方损失下 = yᵢ - f_{m-1}(xᵢ)  （就是残差）

    Step 2 · 拟合一棵 CART 回归树

        以 {(xᵢ, r̃ᵢₘ)} 为训练集，得到 hₘ(x)
        （树的目标变量是伪残差，不是原始 y）

    Step 3 · 更新累积预测

        f_m(x) = f_{m-1}(x) + η · hₘ(x)
                                ↑
                      学习率：每棵树贡献打折，防过拟合

──────────────────────────────────────────────

Step 4 · 输出强学习器

    F(x) = f_T(x) = f₀(x) + η · Σₘ hₘ(x)
```

**学习率 $\eta$（learning rate，又叫 shrinkage 缩减）**：每棵树的修正量乘以 $\eta$（通常 0.01-0.3）再累加。步子小，需要更多棵树，但泛化更好。类比梯度下降的步长：太大容易过冲，太小收敛慢。

> ⚠️ **boosting 两类权重的回归**：GBDT 不调样本权重（区别于 AdaBoost），但学习器权重以"学习率"形式存在——不再按训练误差给每棵树单独算 $\alpha_m$，而是所有树用同一个全局 $\eta$。

### 与提升树的关系：通用 vs 特例

```
                    Boosting 框架
                         │
                  加法模型 + 串行训练
                         │
               ┌──────────────────────┐
               │     提升树           │
               │  （Boosting Tree）   │
               │  拟合"残差"          │
               │  仅适用 平方损失     │
               └──────────┬───────────┘
                          │ 推广：残差 → 负梯度
               ┌──────────▼───────────┐
               │       GBDT           │
               │  拟合"损失函数负梯度" │
               │  支持任意可微损失     │
               └──────────────────────┘
                    ↓ 换损失函数
           分类 / 抗噪回归 / 计数预测 ...
```

### 术语版

| 故事里的元素 | 术语名 | 主场 |
|---|---|---|
| 损失最陡下坡方向 | 负梯度 negative gradient | 本节 |
| 平方损失之外的"差额" | 伪残差 pseudo-residual | 本节 |
| 真实值与预测的差距度量 | 损失函数 loss function | LR 章 |
| 每棵树修正量打折 | 学习率 learning rate（又叫缩减 shrinkage） | 本节 |
| 平方损失下负梯度的别名 | 残差 residual | [`./01-提升树.md`](./01-提升树.md) |

**这一节的关键启示**：**残差只是负梯度在平方损失下的特例 —— 换损失函数，方向还在**。

→ 下一步：[`./03-例子.md`](./03-例子.md) —— 把负梯度公式落到一个 5 样本回归的逐轮手算上

> Sources：
> - PPT Slide 54-55, 62，补充 Slide 13-14
> - Friedman, J.H. (1999) "Greedy Function Approximation: A Gradient Boosting Machine"（负梯度=伪残差的原始表述）
> - 不同损失函数负梯度对照：sklearn GBDT 文档（`loss` 参数说明）
