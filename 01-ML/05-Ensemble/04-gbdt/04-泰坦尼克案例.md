---
tags: [代码/sklearn, 算法/集成学习/GBDT, 案例/泰坦尼克, 实践/分类]
---

# 泰坦尼克号案例（GBDT 分类）

> 维度：**代码**
> 知识点级别：【实践】`GradientBoostingClassifier` API + 完整 pipeline
> 章节底稿全文见 [`README.md`](./README.md)（PPT Slide 63-64）

## ━━━━━━━━ 底稿 ━━━━━━━━

### PPT

> Slide 63 · 梯度提升树——案例泰坦尼克号（代码 1）

```python
# 导入库
import pandas as pd
from sklearn.model_selection import train_test_split
from sklearn.tree import DecisionTreeClassifier
from sklearn.ensemble import GradientBoostingClassifier
from sklearn.metrics import classification_report
from sklearn.model_selection import GridSearchCV

def dm01_gbdtapi():
    # 1 读数据到内存
    taitan_df = pd.read_csv('./data/titanic/train.csv')

    # 2 数据基本处理准备
    # 2-1 x y
    x = taitan_df[['Pclass', 'Age', 'Sex']].copy()
    y = taitan_df['Survived'].copy()

    # 2-2 缺失值处理
    x['Age'].fillna(x['Age'].mean(), inplace=True)

    # 2-3 pclass 离散型数据需 one-hot 编码
    x = pd.get_dummies(x)
```

> Slide 64 · 梯度提升树——案例泰坦尼克号（代码 2）

```python
    # 2-4 数据集划分
    x_train, x_test, y_train, y_test = train_test_split(x, y, random_state=22, test_size=0.2)

    # 3 GBDT 训练和评估
    estimator = GradientBoostingClassifier()
    estimator.fit(x_train, y_train)
    mysorce = estimator.score(x_test, y_test)
    print("gbdt mysorce-->1", mysorce)

    # 4 GBDT 网格搜索交叉验证
    estimator = GradientBoostingClassifier()
    param = {"n_estimators": [100, 110, 120, 130], "max_depth": [2, 3, 4], "random_state": [9]}
    estimator = GridSearchCV(estimator, param_grid=param, cv=3)
    estimator.fit(x_train, y_train)
    mysorce = estimator.score(x_test, y_test)
    print("gbdt mysorce-->2", mysorce)
    print(estimator.best_estimator_)
```

### 笔记

> 【实践】泰坦尼克号案例

```python
from sklearn.ensemble import GradientBoostingClassifier

gbdt = GradientBoostingClassifier(
    n_estimators=100,
    learning_rate=0.1,
    max_depth=3,
)
gbdt.fit(X_train, y_train)
```

**`learning_rate` 直觉**：对应梯度下降的步长 $\eta$，每棵新树的输出乘以 $\eta$ 再累加（shrinkage）。越小越稳但需要更多树。

**对比**：在随机森林案例上替换分类器即可，注意 GBDT 串行训练比 RF 慢。

---

## ━━━━━━━━ 讲解 ━━━━━━━━

### 生活锚 · 同一组人换不同的题

**业务痛点**：前 3 节都在回归场景练手（预测时长、商品价、年龄），但工业界 80% 的需求是**分类**——会不会流失、会不会违约、会不会存活。如果"GBDT 只能回归"，它就只是个小众选手；如果分类机制要从头设计，又跟前 3 节对不上。

**生活锚（接力医生类比换分类版）**：还是体检中心 3 位接力医生，但这次任务从"估体重"换成"估这位病人有多大概率心脏病"。**接力规则不变**——第 1 位先估个概率（比如 0.3），护士告诉他"实际是病人，标签 1，你低估了 0.7"；第 2 位补这 0.7 的差……唯一变的是**护士手里那把"差距尺"**——估体重时是公斤数，估概率时换成"对数损失尺"，但谁估、谁补、累加这套机制完全不动。

> 关键启示：**GBDT 是一套通用接力机制 —— 换损失函数 ≠ 换算法，只是换尺子**。

一句话：**回归换分类，GBDT 只动损失函数，框架不动**。下面用泰坦尼克数据集（虚构示意以外的真实公开数据集）跑一遍 sklearn API。

### 业务问题

泰坦尼克数据集要预测 `Survived`（0 = 死亡，1 = 存活）——典型二分类。要用回归版的 GBDT 直接套上去会出两个问题：

1. **输出对不上**：累加多棵树是个连续值（比如 0.7、−0.3），但需要的是 0/1
2. **损失对不上**：平方损失对"概率"不够敏感（预测概率 0.6 还是 0.9 时，平方损失差距远小于对数损失）

GBDT 给分类的标准做法：

```
回归 GBDT                        分类 GBDT
────────────────                 ──────────────────
损失：平方损失 ½(y-f)²           损失：对数损失 -y·log(p)-(1-y)·log(1-p)
负梯度：y - f（残差）            负梯度：y - p（标签 - 预测概率）
累加 f：连续值,直接当预测        累加 f：先 sigmoid 把它压到 (0,1) 当概率
                                   超过 0.5 → 类别 1
```

sklearn 的 `GradientBoostingClassifier` 默认就用对数损失（`loss='log_loss'`），传 0/1 标签直接跑——使用者不需要手动接管损失函数。下面看代码。

### 【代码】关键参数扫盲

| 参数 | 默认值 | 直觉 | 调参方向 |
|---|---|---|---|
| `n_estimators` | 100 | 弱学习器（树）的个数；越多越准但越慢 | 先粗调：50/100/200；再精调 |
| `learning_rate` | 0.1 | 每棵树贡献的缩放系数（shrinkage）；小则稳，需配合大 n_estimators | 0.05-0.3；通常与 n_estimators 联动 |
| `max_depth` | 3 | 每棵树的最大深度；GBDT 弱树不需要很深 | 3-5；太深会过拟合 |
| `subsample` | 1.0 | 每轮随机抽多少比例样本建树（随机梯度）；<1.0 引入随机性防过拟 | 0.6-0.9，配合小 learning_rate |
| `min_samples_split` | 2 | 节点继续分裂的最少样本数 | 增大可防过拟合 |
| `random_state` | None | 随机种子，保证可复现 | 固定一个值即可 |

**`learning_rate` 和 `n_estimators` 是跷跷板**：

```
learning_rate 小  ←→  n_estimators 大
learning_rate 大  ←→  n_estimators 小

经验：先固定 learning_rate=0.1，调 n_estimators；
      确定棵数后再调小 learning_rate、增大棵数做精调
```

### 【代码】完整 Pipeline（按 sklearn 标准模板补全）

PPT 代码片段拼合成可运行的完整版本。数据预处理与 PPT 保持一致（Pclass + Age + Sex，Age 均值填充，get_dummies 编码）。

```python
import pandas as pd
from sklearn.model_selection import train_test_split, GridSearchCV
from sklearn.ensemble import GradientBoostingClassifier, RandomForestClassifier, AdaBoostClassifier
from sklearn.metrics import classification_report

# ── 1. 读数据 ──────────────────────────────
df = pd.read_csv('./data/titanic/train.csv')

# ── 2. 特征工程 ────────────────────────────
x = df[['Pclass', 'Age', 'Sex']].copy()
y = df['Survived'].copy()

# 缺失值：Age 用均值填充（PPT 原方案）
x['Age'].fillna(x['Age'].mean(), inplace=True)

# 类别特征：Sex（male/female） + Pclass（1/2/3）用 get_dummies
# get_dummies 会把 Sex 拆成 Sex_female / Sex_male 两列
x = pd.get_dummies(x)

# ── 3. 划分训练/测试集 ─────────────────────
x_train, x_test, y_train, y_test = train_test_split(
    x, y, random_state=22, test_size=0.2
)

# ── 4. 默认参数跑一遍（基线） ───────────────
gbdt = GradientBoostingClassifier(random_state=42)
gbdt.fit(x_train, y_train)
print(f"GBDT 默认参数准确率: {gbdt.score(x_test, y_test):.4f}")

# ── 5. 网格搜索调参（PPT 方案） ─────────────
param_grid = {
    "n_estimators": [100, 110, 120, 130],
    "max_depth": [2, 3, 4],
    "random_state": [9],
}
gs = GridSearchCV(
    GradientBoostingClassifier(),
    param_grid=param_grid,
    cv=3,
    n_jobs=-1,   # 并行加速
    verbose=1,
)
gs.fit(x_train, y_train)
print(f"GBDT 调参后准确率:   {gs.score(x_test, y_test):.4f}")
print(f"最优参数:             {gs.best_params_}")

# ── 6. 与 RF / AdaBoost 对比 ───────────────
models = {
    "Random Forest": RandomForestClassifier(n_estimators=100, random_state=42),
    "AdaBoost":      AdaBoostClassifier(n_estimators=100, random_state=42),
    "GBDT":          GradientBoostingClassifier(n_estimators=100, random_state=42),
}
print("\n── 模型准确率对比 ──")
for name, model in models.items():
    model.fit(x_train, y_train)
    score = model.score(x_test, y_test)
    print(f"  {name:<16}: {score:.4f}")
```

**预期输出**（数值因随机种子略有浮动，趋势稳定，**虚构示意**实际复现可能略有差异）：

```
GBDT 默认参数准确率: ~0.816
GBDT 调参后准确率:   ~0.821

── 模型准确率对比 ──
  Random Forest   : ~0.804
  AdaBoost        : ~0.793
  GBDT            : ~0.816
```

三种 Boosting/Bagging 模型在同一数据集上，GBDT 通常略优，但差距不大——差别在 1-3 个百分点。

### GBDT vs RF vs AdaBoost：工程对比

| 维度 | Random Forest（RF） | AdaBoost | GBDT |
|---|---|---|---|
| 训练方式 | **并行**（各树独立） | **串行**（依赖上一轮权重） | **串行**（依赖上一轮残差） |
| 训练速度 | 快（可多核） | 中 | 慢（纯串行，无法并行） |
| 推理速度 | 快 | 快 | 快（推理时各树独立） |
| 防过拟合机制 | 随机特征 + Bagging | 样本权重 + 早停 | learning_rate + max_depth + subsample |
| 离群点鲁棒性 | 一般 | 差（离群点被反复加权） | 可选 Huber/MAE 损失 |
| 准确率上限 | 中高 | 中 | **高**（工业界基准之一） |
| 调参难度 | 低（n_estimators + max_features） | 低 | 中（多参数联动） |
| 可解释性 | 中（feature importance） | 中 | 中（feature importance） |

```
训练流程对比

RF：  tree₁ ──┐
      tree₂ ──┼── 投票/平均 → 输出
      tree₃ ──┘
      （并行，各树独立，快）

GBDT：f₀ → tree₁ → f₁ → tree₂ → f₂ → tree₃ → ...
      （串行，每棵树依赖前一轮结果，慢）
      
推理时 GBDT 和 RF 一样快：都是过 T 棵树然后累加/平均。
慢在训练，不在推理。
```

**工程结论**：数据量不大、追求精度 → GBDT；数据量大、追求训练速度 → RF；GBDT 精度想要但训练太慢 → 下一章 XGBoost（GBDT 工程化升级，支持并行）。

### 常见误区

- **`GradientBoosting` 比 `RandomForest` 一定准** —— 多数任务上略好 1-3 个点，但小数据 / 强噪声场景 RF 更稳；先跑 RF 拿基线，GBDT 涨幅不显著就别上
- **盯 `n_estimators` 调参** —— `n_estimators` 与 `learning_rate` 是跷跷板，单调一边几乎没意义；推荐先固定 `learning_rate=0.1` 调棵数，再压学习率配大棵数做精调
- **不开 `random_state`** —— 默认 None，每次运行结果不一致，没法复现；调参对比时**必须**固定一个值
- **`subsample=1.0` 一直裸跑** —— 默认值不引入随机性；想要更强泛化时把 `subsample` 调到 0.6-0.9，配合小学习率，效果接近随机梯度提升

### 不在本章范围

- **GBDT 训练为什么慢、能不能并行** → [`../05-xgboost/`](../05-xgboost/) XGBoost 在单棵树的特征分裂粒度上做并行
- **GBDT 类别特征不需要 one-hot 的版本** → LightGBM / CatBoost（本套教材范围外）

### 术语版

| 故事里的元素 | 术语名 | 主场 |
|---|---|---|
| 二分类的损失尺子 | 对数损失 log loss / 交叉熵 cross entropy | LR 章 |
| 累加输出压到 (0,1) | sigmoid 函数 | LR 章 |
| sklearn 的 GBDT 分类器 | `GradientBoostingClassifier` | 本节 |
| 每棵树修正量打折 | 学习率 learning rate（缩减 shrinkage） | [`./02-梯度提升树.md`](./02-梯度提升树.md) |
| 每轮抽样比例 | 子采样 subsample（随机梯度提升 SGB） | 本节 |

**这一节的关键启示**：**换损失不换框架 —— GBDT 是一套通用接力机制，回归分类同源**。

→ 下一步：[`../05-xgboost/`](../05-xgboost/) —— GBDT 的工程化升级（二阶梯度 + 正则项 + 单树并行）

> Sources：
> - PPT Slide 63-64（泰坦尼克 GBDT 代码）
> - sklearn `GradientBoostingClassifier` 文档（参数说明，按 sklearn 标准补全）
> - 完整 pipeline 按 sklearn 标准模板补全（PPT 代码片段为基础）
